# Dependencies
node_modules/

# Build outputs
dist/
build/
.svelte-kit/

# D6 — packages/vault-action/dist/ is the one exception: it is the literal artifact GitHub
# Actions executes for every `uses: project-vault/vault-action@v1` consumer (the Actions runtime
# never runs an install step), so it must be committed. Kept fresh by the CI check in
# scripts/check-vault-action-dist-fresh.ts — do not "clean this up" by re-ignoring it.
!packages/vault-action/dist/
!packages/vault-action/dist/**

# Coverage
coverage/
.nyc_output/

# Story 22.4 — scripts/audit-quota-isolation-bench.ts's JSON artifacts (raw, per-run measurement
# data; the human-readable summary lives in docs/operations/audit-quota-degradation-strategy.md)
scripts/.bench-output/

# Stryker
.stryker-tmp/
reports/

# Environment files
.env*
!.env.example
!apps/web/e2e/.env.test.example

# Playwright (Story 10-1)
apps/web/e2e/test-results/

# Editor directories
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Turbo
.turbo/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local assistant artifacts
.claude/
.codex/
.playwright-mcp/
.worktrees/
prompts.md

# TypeScript
*.tsbuildinfo

# Misc
*.tmp

# Vault key material (envelope/file mode halves) — never commit secrets
dev-secrets/*
!dev-secrets/.gitkeep

# Story 11-1 — raw, unprocessed brand source uploads (1024x1024 originals, ~1.4-1.5MB each).
# The derived, production-ready assets they were cropped/resized into live in
# apps/web/static/ (logo.png, icon.png, favicon.png, apple-touch-icon.png) and ARE committed —
# the app references them directly. These originals are only needed if the assets are ever
# regenerated from scratch, not to build or run the app, so they stay out of version control.
design/brand-source/
/project-vault-logo.png
/project-vault-icon.png
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml

# Private BMAD companion overlay. These paths are linked into a public checkout only by
# project-vault-private/scripts/attach-public.sh and must never enter the public repository.
.agents/
.bmad-loop/
_bmad/
_bmad-output/
.github/skills/
.github/agents/
.github/workflows/post-merge-status-drift.yml
AGENTS.md
CLAUDE.md
design-artifacts/
docs/federated-multi-tenant-architecture-analysis.md
docs/usability-audit-2026-07-13.md
scripts/check-alert-pending-epic3.test.ts
scripts/check-alert-pending-epic3.ts
scripts/check-operational-log-language.ts
scripts/check-post-merge-status-drift.test.ts
scripts/check-post-merge-status-drift.ts
scripts/check-psc-tbd-tracking.test.ts
scripts/check-psc-tbd-tracking.ts
scripts/check-sprint-status-rollup.test.ts
scripts/check-sprint-status-rollup.ts
scripts/check-story-references.test.ts
scripts/check-story-references.ts
scripts/check-story-review-deferrals.test.ts
scripts/check-story-review-deferrals.ts
scripts/check-story-status-sync.test.ts
scripts/check-story-status-sync.ts
