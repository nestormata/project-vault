# @project-vault/extension-api

## License & Publishing

This package is `private: true` and inherits the repo's `AGPL-3.0-or-later` license today —
it is not published to any registry and is consumed only via in-monorepo `workspace:*`
references. See `architecture.md`'s **Publish Readiness** subsection (`#### Publish Readiness`,
immediately after the "Extension API package" bullets under `### API & Communication Patterns`)
for the full rationale, the license/AGPLv3 boundary explanation, and the concrete trigger
condition for revisiting external/registry publishing.
