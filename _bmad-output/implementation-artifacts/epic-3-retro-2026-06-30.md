# Epic 3 Retrospective — Notification Infrastructure

**Date:** 2026-06-30  
**Epic:** 3 — Notification Infrastructure — Alert Delivery, Routing & In-App Inbox  
**Facilitator:** Bob (Scrum Master)  
**Project Lead:** Nestor  
**Status:** Stories 3/3 marked `done` in sprint-status; **epic `in-progress`** — G2 product-surface gate **not passed**  
**Type:** Epic retrospective with inconsistency / gap audit

---

## Executive Summary

Epic 3 delivered **notification infrastructure end-to-end in code**: `notification_queue`, email/Slack/inbox delivery, PENDING_DELIVERY backfill (FR73), preferences and org routing (FR94/FR100), settings UI, SSE inbox push, global nav badge, and inbox page at `/notifications`.

However, **epic completion is blocked** by product-surface gaps that repeat Epic 2 patterns: orphaned `/alerts` placeholder, hardcoded dashboard alert counts, unfilled Epic 1 MFA alert stubs, empty Story 3.3 Dev Agent Record, and P3 status drift on Story 3.1.

**Nestor (Project Lead) scope:** Retrospective plus explicit audit of inconsistencies, contradictions, and missing work across PRD, epics, architecture, sprint-status, story files, and implementation.

**Epic 3 verdict (today):** **Not done** — keep `epic-3: in-progress` until G2 gate items below are closed.

**Epic update required before Epic 4?** No fundamental architecture change for invitations. **Epic 3 needs a small closure pass** (navigation truth, MFA wiring, dashboard counts) before Epic 4 evaluator demos that assume alerts work in the UI.

---

## Delivery Metrics

| Metric | Value |
|--------|-------|
| Stories (sprint-status) | 3/3 `done` (3.1, 3.2, 3.3) |
| Epic status | `in-progress` (correct — G2 not met) |
| Story file vs sprint drift | 3.1 file `Status: review`; sprint `done` |
| FR51 / FR52 | ✅ Email + Slack delivery |
| FR94 / FR100 | ✅ Preferences + routing API + settings UI |
| FR107 | ⚠️ Inbox implemented at `/notifications`; `/alerts` placeholder orphaned |
| FR73 failed-auth | ✅ Live + backfill tested |
| Epic 1 MFA alert stubs | ❌ Still `alert.pending_epic3` in `mfa.ts` |
| G2 epic gate | ❌ Not passed |
| Technical debt | Dashboard counts hardcoded; MFA alerts; AC-E3a UI gap; 3.3 Dev Record empty |

---

## What Went Well

1. **Solid delivery pipeline (3.1)** — Outbox pattern (`orgId` in pg-boss payload, post-commit send), RLS-safe workers, nodemailer + Slack webhook, admin test API, FR73 backfill with tests.

2. **Dispatcher maturity (3.2)** — Computed defaults, per-org preferences, routing with zero-member fallback, digest scheduling via `deliver_at`, unified `notification:deliver` worker (ADR-3.2-01).

3. **Web surface on 3.2** — `/settings/notifications` shipped with API in same story (Epic 2 lesson applied).

4. **Inbox implementation exists (3.3)** — `notification_inbox` table, inbox worker + SSE `notification.inbox`, purge job, inbox CRUD API, `notifications.svelte.ts` state, nav badge in `+layout.svelte`.

5. **Architecture conflict resolution in story files** — Job naming, SSE endpoint (`GET /api/v1/stream`), SMTP env vs Epic 9 documented per story.

6. **E3-P2 partially honored** — Inbox code shipped before epic marked `done`; epic correctly remains `in-progress`.

7. **Nav updated to real inbox** — `nav-model.ts` href is `/notifications` (matches implemented page).

---

## Challenges & Patterns

### Recurring Struggles

| Pattern | Where | Impact |
|---------|-------|--------|
| Sprint `done` before product-surface truth | 3.3 vs `/alerts` placeholder | G3 violation — dead route + stale placeholder test |
| Epic 1 deferrals partially closed | `mfa.ts` | FR73 live; MFA recovery alerts still silent |
| P3 story file vs sprint-status drift | 3.1 | `review` vs `done` |
| Dashboard counts not wired | `dashboard-stats.ts`, `projects/routes.ts` | Honest zeros outdated now that inbox exists |
| Dev Agent Record not filled | 3.3 | Loses retro intelligence; violates story workflow |
| AC-E3a vs implementation | epics vs code | Settings page has no "send test" — admin API only |
| Planning doc tier note vs G2 gate | `epics.md` beta cut | Confusing whether inbox is optional for epic done |

### Breakthrough Moments

- **Inbox delivery requires EventEmitter injection** — `deliverNotification(..., emitter)` pattern in `notification-deliver.ts` cleanly wires SSE post-commit.
- **Story 3.3 architecture table** — Correctly maps epic prose to canonical `GET /api/v1/stream`.
- **Epic 2 guardrails caught epic premature close** — `epic-3` still `in-progress` despite all stories `done`.

---

## Previous Retro Follow-Through

### Epic 2 (2026-06-30)

| Action | Status | Notes |
|--------|--------|-------|
| E3-P1 Surface contracts 3.1–3.3 | ✅ | Present in all story files |
| E3-P2 No epic `done` without inbox | ⏳ | Inbox code exists; **G2 still failing** on nav/placeholder/dashboard |
| E3-P3 / E3-1 SMTP decision | ⏳ | Env vars implemented in 3.1 prose; **no ADR closure** in `deferred-work.md` |
| E3-2 FR73 integration test | ✅ | `notification-backfill.test.ts` |
| D1 architecture.md naming | ❌ | Still open |
| G1–G4 guardrails | ✅ | Process templates updated |

### Epic 1 (2026-06-30)

| Commitment | Status |
|------------|--------|
| Wire `alert.pending_epic3` in Epic 3 | ⚠️ Partial — FR73 only |
| Update `mfa-policy-matrix.md` | ❌ Still says stub until 3.1 |

---

## Inconsistency, Contradiction & Gap Audit

Severity: 🔴 blocks `epic-3: done` | 🟠 should fix soon | 🟡 doc/hygiene | ⚪ tracked deferral

### A. Epic completion & G2 gate

| ID | Finding | Severity | Evidence |
|----|---------|----------|----------|
| A1 | All stories `done` but epic `in-progress` | 🟡 | Correct per G2 — **do not flip epic to `done` yet** |
| A2 | **`/alerts` still placeholder** while nav uses `/notifications` | 🔴 | `apps/web/src/routes/(app)/alerts/+page.svelte` → `PlaceholderSection`; `nav-model.ts` → `/notifications` |
| A3 | `auth-guard.ts` lists `/alerts` as protected route | 🟠 | Orphan route; should redirect to `/notifications` or be removed |
| A4 | `placeholder-sections.test.ts` expects alerts "Epic 3" copy | 🔴 | Stale — inbox shipped |
| A5 | Story 3.3 Dev Agent Record empty despite `Status: done` | 🟠 | P3 + retro intelligence loss |
| A6 | Story 3.1 file `Status: review` vs sprint `done` | 🟠 | P3 violation |

### B. PRD / epics / story vs code

| ID | Contradiction | Severity | Resolution |
|----|---------------|----------|------------|
| B1 | Story 3.3 persona says **`/alerts`**; implementation uses **`/notifications`** | 🟡 | Nav aligned to `/notifications` — **update story persona + deferred-work**; add `/alerts` → `/notifications` redirect |
| B2 | AC-E3a: test from **notification settings page** | 🟠 | Only `POST /api/v1/admin/notifications/test` exists |
| B3 | Epic 1.8: `mfa.recovery_used` email when Epic 3 live | 🔴 | `mfa.ts` still logs `alert.pending_epic3`; types missing from `NOTIFICATION_ALERT_TYPES` |
| B4 | `mfa-policy-matrix.md`: stub until 3.1 | 🟡 | Update: FR73 live; MFA pending |
| B5 | `epics.md` T2 beta cut defers FR107 inbox | 🟡 | Clarify: deferrable for **external beta tier**, not for **internal epic done** (G2) |
| B6 | Story 3.1 linked UI "3-2 test UI" | 🟠 | Settings has no test button |
| B7 | `deferred-work.md`: `/alerts` placeholder until 3.3 | 🔴 | Stale — update or close via redirect |

### C. Implementation gaps

| ID | Finding | Severity | Location |
|----|---------|----------|----------|
| C1 | `alertCount` / `unresolvedAlertCount` hardcoded `0` | 🔴 | `projects/routes.ts`, `dashboard-stats.ts` |
| C2 | Credential expiry **notification jobs** not wired | ⚪ | Deferred — columns from 2.4 |
| C3 | `notification_queue` failed/DLQ cleanup deferred | 🟡 | 3.1 Dev Record |
| C4 | N+1 preference queries in dispatcher | 🟡 | TODO in dispatcher (3.2 pre-mortem) |

### D. Epic 4 readiness

| ID | Dependency | Status |
|----|------------|--------|
| E1 | Invitation emails need SMTP | ✅ 3.1 |
| E2 | MFA journey for invites | ✅ Epic 1 |
| E3 | Evaluator expects working alerts UI | ⚠️ Fix A2/A4/C1 before demo |
| E4 | Epic 4 API can start in parallel | ✅ If PO accepts Epic 3 closure debt temporarily |

---

## Significant Discovery Alert

### 1. "All stories done" ≠ epic done (G2 working)

Sprint-status shows 3.1–3.3 `done` but correctly keeps `epic-3: in-progress`. This is the **intended G2 behavior** Epic 2 retro demanded.

### 2. Navigation truth regression (G3)

Implementing inbox at `/notifications` without removing `/alerts` placeholder repeats Epic 2's "link resolves" failure in reverse: nav works, but **bookmark/direct `/alerts` hits stale placeholder** and tests still assert Epic 3 deferral copy.

### 3. Epic 1 alert stub checklist incomplete

Failed-auth path closed in 3.1; **MFA recovery paths were never added to Epic 3 scope tracking** despite epics.md and Story 1.8 AC explicitly promising email on recovery.

### 4. Dashboard truth lag (again)

Story 2.8 fixed credential counts; alert counts were honestly `0` until Epic 3. Inbox and security alerts now exist — **hardcoded zeros are no longer honest placeholders; they are incorrect.**

---

## Readiness Assessment

| Area | Status |
|------|--------|
| Email/Slack delivery | ✅ |
| FR73 failed-auth | ✅ |
| Preferences + routing | ✅ |
| Inbox backend + SSE + badge | ✅ |
| Inbox web page | ✅ at `/notifications` |
| Nav / route truth (G3) | ❌ `/alerts` orphan |
| Dashboard alert counts | ❌ Still `0` |
| MFA recovery alerts | ❌ Stubs remain |
| AC-E3a settings test UI | ❌ |
| Story tracking hygiene | ⚠️ 3.1 P3, 3.3 Dev Record |
| **G2 epic gate** | **❌ Not passed** |

---

## Action Items

### Critical path — before `epic-3: done`

| # | Action | Owner | Success criteria |
|---|--------|-------|------------------|
| CP-1 | `/alerts` → `/notifications` redirect **or** move inbox to `/alerts` | Dev | No placeholder at user-facing alerts URL |
| CP-2 | Update/remove stale placeholder test + `deferred-work.md` `/alerts` row | Dev / SM | CI green; docs accurate |
| CP-3 | Wire `alertCount` / `unresolvedAlertCount` from inbox + `security_alerts` | Dev | Dashboard truth when data exists |
| CP-4 | Wire MFA alerts: add types + dispatcher calls in `mfa.ts` | Dev | No `alert.pending_epic3` in MFA paths |
| CP-5 | Fill Story 3.3 Dev Agent Record; sync 3.1 status (P3) | Dev / SM | Story files match sprint |

### Process

| # | Action | Owner |
|---|--------|-------|
| P3-1 | Add grep checklist for `alert.pending_epic3` to epic-close gate | SM |
| P3-2 | Reconcile `epics.md` Epic 3 beta-cut vs G2 inbox requirement | PO |
| P3-3 | Close E3-1 in `deferred-work.md` (env vars MVP, Epic 9 UI later) | Architect |

### Documentation

| # | Action | Owner |
|---|--------|-------|
| D3-1 | Update `mfa-policy-matrix.md` alert delivery row | Tech Writer |
| D3-2 | Update Story 3.3 persona journey to `/notifications` | SM |

### Optional polish (not blocking if AC-E3a amended)

| # | Action | Owner |
|---|--------|-------|
| O-1 | "Send test notification" on `/settings/notifications` (admin) | Dev |
| O-2 | Credential expiry notification jobs | Epic 3.x / later |

---

## Epic 4 Preview

Epic 4 (Team & Organization Management) depends on:

- **SMTP for invitation emails** — ready via 3.1
- **MFA enrolled before invites (FR57)** — Epic 1 + Story 4.1 AC
- **No hard dependency on inbox** for 4.1 API

**Recommendation:** Complete Epic 3 closure items CP-1–CP-4 before evaluator-facing Epic 4 demos; API work can proceed in parallel.

---

## Team Agreements

1. **`epic-N: done` requires G2 gate**, not story count alone.
2. **Orphan routes are G3 failures** — redirect or delete placeholders when real routes ship.
3. **Cross-epic deferrals need a callsite checklist** (`grep alert.pending_epic3`).
4. **Dev Agent Record is part of done** — empty record = incomplete story hygiene.
5. **Honest zeros expire** when backing data exists — dashboard must update.

---

## Key Takeaways

1. Epic 3 **infrastructure is strong** — queue, multi-channel delivery, preferences, inbox, SSE.
2. **G2 gate caught incomplete product surface** — same class of issue Epic 2 retro predicted.
3. **MFA alert wiring is a real gap**, not documentation drift.
4. **`/alerts` vs `/notifications`** — implementation chose `/notifications`; cleanup pass required.
5. **Dashboard alert counts** must follow inbox delivery — repeat of 2.8 dashboard truth lesson.

---

## Next Steps

1. Execute **CP-1 through CP-5** (small closure story 3.4 or amend 3.3 follow-up — Nestor's call).
2. Set `epic-3: done` only after G2 checklist passes + update `deferred-work.md`.
3. Update planning docs (B4, B5, D3-1).
4. Begin Epic 4.1 when PO confirms Epic 3 closure acceptable for invite email path.

---

*Generated by bmad-retrospective workflow — Epic 3, Project Vault. Includes inconsistency / gap audit.*
