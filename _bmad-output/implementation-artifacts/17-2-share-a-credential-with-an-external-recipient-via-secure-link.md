# Story 17.2: Share a Credential with an External Recipient via Secure Link

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a project member with reveal access to a credential,
I want to share that credential's current value — or a specific field of a multi-field secret — with someone **outside my organization** (no account, no login) via a bounded-duration, single-use-or-time-boxed link that requires me to step up my own authentication before I can create it,
so that I can hand a secret to an external contractor/vendor/support engineer without giving them a Project Vault account, while the product still gives me full audit visibility and my org's admins stay informed that an unauthenticated access pathway to secret material now exists.

## Product Surface Contract

> Required. Rules: `_bmad-output/implementation-artifacts/product-surface-contract.md`

| Field | Value |
|-------|-------|
| **Surface scope** | `both` |
| **Evaluator-visible** | yes |
| **Linked UI story** (if API-only) | N/A — UI ships in this story |
| **Honest placeholder AC** (if UI deferred) | N/A |
| **Persona journey** | See below |

### Persona journey stub

**Morgan (project member)** opens a credential detail page for a credential they can reveal, clicks **Share**, toggles the recipient type from "org member" (17.1) to **"external (email)"**, types the recipient's email address, sets an expiry (or accepts the default — shorter than 17.1's, see AC-5), optionally toggles "single view only" (defaults to `true` and cannot be turned off for external recipients — see AC-5), and clicks **Create share link**. Before the share is created, Morgan is prompted to **re-enter their password or a fresh MFA code** (step-up re-authentication, AC-3) — creating an unauthenticated access pathway to secret material is treated as sensitive enough to require this even though normal reveal doesn't. Once step-up succeeds, Morgan sees the generated link once (masked/copy-once affordance) and a confirmation that no in-app notification goes to the external recipient (Morgan must send the link themselves, e.g. via email) but that org admins have been notified a new external share was created.

**Priya (the external recipient, has no Project Vault account)** receives the link (out of band — email, Slack, however Morgan sends it) and opens it in a browser with **no login prompt**. Priya lands on a public, unauthenticated **consent page** that names the credential, the sharer, and the field (if scoped) but shows no secret value and performs no state-changing action on load — a chat-client link-unfurling bot or an email "safe link" prefetcher that fetches this same URL automatically burns nothing (AC-9). Priya clicks an explicit **"Reveal secret"** button, which POSTs to the reveal endpoint and shows the value. If the link is single-use (the default and, for external recipients, the only option), a second open of the same link shows "This link has already been used" instead of the value. If the window has expired, Priya sees "This link has expired." The page sets `Referrer-Policy: no-referrer` on its own document response (not just the API) and loads no third-party resources, so nothing about the token-bearing URL can leak via an outbound `Referer` header (AC-10, F1).

Org admins receive an in-app/email alert (existing FR100 routing) both when Morgan creates the external share and again the first time Priya actually views it (AC-12) — creation-only notification would leave admins blind to whether a live external link was ever actually exercised.

Morgan's existing **Shares** tab (built in 17.1) now also lists external shares mixed in with member shares, showing the recipient's email instead of a member name, and offers the same **Revoke** action.

## Acceptance Criteria

1. **Recipient is an external email address, not an org member.** The share-creation endpoint (a new sibling to 17.1's `POST /:projectId/credentials/:credentialId/shares`, or an extension of the same endpoint accepting a `recipientType` discriminator — dev agent's call, documented in the Dev Agent Record) accepts `recipientEmail` (a validated email address) instead of `recipientUserId`. The created row has `recipient_type = 'external'`, `recipient_email` set to the (lowercased/trimmed) address, and `recipient_user_id = NULL`. Example: Morgan shares field `api_key` of credential "Stripe Prod" with `priya@vendor.example` — the row has `recipient_type='external'`, `recipient_email='priya@vendor.example'`, `recipient_user_id=NULL`, `field_key='api_key'`. Edge case: an email that happens to match an existing org member's address is still stored as an external share (this endpoint never resolves email → `recipient_user_id`; if Morgan actually meant to share with a teammate, that's 17.1's `recipientUserId` flow, and mixing the two is a UI-level user-education problem, not something this endpoint silently "fixes" by resolving the address).

2. **Share creation eligibility reuses the same reveal gate as 17.1 (and normal reveal).** A user can create an external share only if their effective project role would pass `rejectIfInsufficientProjectRoleForReveal(..., kind: 'reveal')` (`apps/api/src/modules/credentials/routes.ts`) — identical to 17.1's AC-1, no looser check for the external path even though it's a different recipient type. Example: a `viewer`-role project member attempting to create an external share gets the same `403 INSUFFICIENT_PROJECT_ROLE` a `viewer` gets attempting normal reveal or an internal share.

3. **Sharer step-up re-authentication is required at creation time.** This is new versus 17.1 and is the epic security review's Round 1 finding, applied here for the first time in this codebase (no existing step-up/re-auth primitive exists — confirmed by codebase search; the closest analogues are login-time password verification (`apps/api/src/modules/auth/password.ts#verifyUserPassword`) and login-time TOTP verification (`apps/api/src/modules/auth/mfa-login.ts#verifyConfirmedLoginTotp`)). Concretely: the create-external-share request body must include **either** a `password` field (verified against the sharer's own stored hash via `verifyUserPassword`) **or**, if the sharer has MFA enrolled, a `totpCode` field (verified via the same TOTP-verification primitive `mfa-login.ts` uses at login, reused rather than reinvented) — the caller supplies whichever factor they have. This is a **synchronous, stateless re-check on this single request** (no new "step-up session"/short-lived elevated-privilege token is introduced — that would be meaningfully more work and is explicitly out of scope; document this as a deliberate scope decision in the Dev Agent Record, the same way 17.1 flagged its "no MFA on share creation" decision). A missing/incorrect factor returns `401 step_up_required` (distinct code from generic auth failure) with no share created and no partial side effects. **Failed step-up attempts are rate-limited (surfaced by Failure Mode Analysis during advanced elicitation):** this endpoint accepts a live password/TOTP guess from an already-authenticated session, which is a brute-force surface against the sharer's own credential that didn't previously exist at this granularity (login has its own rate limiting, but this is a *second*, independent guessing endpoint for the same secret) — reuse this codebase's existing `secureRoute` `rateLimit` mechanism (same shape 17.1 used for its revoke route) scoped tightly on this route, e.g. a handful of attempts per minute per session, returning `429` once exceeded rather than continuing to accept guesses. This is distinct from AC-22's per-token reveal-attempt cap (that one protects the *recipient-facing* reveal endpoint; this one protects the *sharer's own credential* from being brute-forced via the creation endpoint). **If the sharer has no MFA enrolled**, the `password`-only path is accepted (password re-entry is always available; MFA is an *additional* accepted factor, not a hard requirement to use this feature at all — requiring MFA enrollment as a precondition would make external sharing unusable for any org that hasn't rolled out MFA, which is out of proportion to this story's scope). Example: Morgan (no MFA enrolled) supplies their current password in the request body; it verifies; share creation proceeds. Counter-example: Morgan supplies a stale/wrong password → `401 step_up_required`, nothing persisted. Counter-example: Morgan has MFA enrolled and supplies an already-used or expired TOTP code → same `401`, reusing the existing TOTP-replay-prevention table (`totpUsedCodes`) rather than a parallel mechanism.

4. **Field scoping reuses 17.1's `field_key` model unchanged.** Same `NULL` = whole-credential, non-null must match a current template field key at creation time, same auto-expire-on-field-removal-or-rename semantics as 17.1's AC-3 (validated the same way, via `validateFieldKey`/`fieldMetaForResponse`, reused verbatim — no new field-validation code path). Example: sharing field `db_password` of a multi-field secret; if that field is later renamed to `database_password` (Story 13.2 rename) before Priya views the link, her next GET/reveal attempt on the old field key resolves as expired (same "treat missing field as expired, never a 500 or silent null" contract 17.1's `revealShare`/`revealCurrentValue` already implements — reused, not reimplemented, for the external path).

5. **Bounded duration, single-use only (tighter than 17.1 by design).** At creation, the sharer sets `expiresAt` (required, future, server-enforced max). **Reasoned default, a judgment call for the dev agent to document in the Dev Agent Record (same convention as 17.1's `SHARE_DEFAULT_TTL_MS`/`SHARE_MAX_TTL_MS`):** default **1 hour**, capped at **72 hours (3 days)** — materially tighter than 17.1's 24h default / 7-day cap, because an external link has no account behind it at all (anyone who obtains the URL — forwarded mail, a compromised inbox, a shared inbox, a "safe link" scanning proxy that stores the URL) can use it, versus 17.1's link which still requires the *correct authenticated org member's session* even if forwarded. **`singleUse` is not a caller-controlled option for external shares — it is hard-coded to `true` server-side** (the request body must either omit it or pass `true`; passing `false` is a `400 external_share_must_be_single_use`), unlike 17.1 where multi-view is a real, honored option. Rationale: a multi-view, long-lived, unauthenticated bearer link is the single highest-risk configuration this feature could produce, and nothing in the product requirement (FR122/FR123) demands multi-view for the external case — 17.1's org-member path already covers the "hand off ongoing access to a known person" use case via an authenticated, revocable, re-checked-at-view session binding that the external path structurally cannot offer. Example: `expiresAt` 30 minutes out, single-use — one successful reveal by Priya, then any further open shows "already been used." Edge case: sharer omits `expiresAt` entirely → `400` (required, same as 17.1); sharer requests 5 days out → `400 expires_at_invalid: too_far_in_future` (72h cap, not 17.1's 7-day cap).

6. **Revocation mirrors 17.1.** The sharer (or any org admin/owner) can revoke an `active`/`pending` external share at any time before it's viewed or expires, reusing `revokeShare` (already generic over `recipient_type` — confirmed by reading `apps/api/src/modules/credential-shares/service.ts`: `revokeShare`/`findShareInScope` filter only by `orgId`/`credentialId`/`shareId`/`status`, never by `recipient_type`). Revoking an already-viewed/expired/revoked share is the same idempotent no-op as 17.1's AC-5. **Reuse `revokeShare`, `findShareInScope`, and the revoke route's now-fixed auth-before-mutate ordering (PR #251, see Previous Story Intelligence) verbatim** — do not write a second revoke function for the external path.

7. **The `credential_shares` table needs no new migration for schema shape** — 17.1's migration `0059_credential_shares.sql` already created `recipient_type`, `recipient_email`, and every other column this story needs. This story's DB-layer work is entirely about **RLS**, not schema: the org-scoped RLS policy on `credential_shares` (applied by migration 0059, standard `orgScoped` pattern) assumes every query runs inside a session that has already called `set_config('app.current_org_id', ...)` — which is only ever set from an **authenticated** request's resolved `orgId` (see `apps/api/src/middleware/rls.ts#setRlsOrgContext`). An external recipient has no session and therefore no `orgId` to set *before* the token lookup resolves which org the share even belongs to — this is the "org unknown until token resolves" problem, and it needs a documented, narrow exception (see AC-8 for the concrete mechanism; this AC is the acknowledgement that one is required, distinct from 17.1's Dev Notes explicitly saying **no** RLS exception was needed there).

8. **Tokenized bearer-link access — no session required, org context established purely from the token.** This is the core architectural difference from 17.1. Concrete mechanism (reusing an existing precedent in this exact codebase, not sessions/refresh_tokens — see Dev Notes for why that precedent is the wrong shape here — but `apps/api/src/modules/monitoring/public-status-page-routes.ts` + `status-page-service.ts#findStatusPageByTokenHash`, Story 6.3's public status-page token pattern): the external access routes are registered with `secureRoute(..., security: { requireAuth: false, ... })` (the same flag `publicStatusPageRoutes` uses). The token-lookup step runs on the **admin DB connection** (`getAdminDb()` from `apps/api/src/lib/db.ts`, which bypasses the org-scoped RLS session variable entirely, exactly as `findStatusPageByTokenHash` does) to find the `credential_shares` row by `token_hash` — this is the one, narrow, explicitly-justified place an unauthenticated request touches this table without RLS scoping, and it is a single indexed point-lookup by hash, not a scan. Once the row (and therefore `orgId`) is resolved, every subsequent read/write in the same request (credential/field lookup, the atomic reveal claim, the audit write) re-scopes via `withOrg(orgId, ...)` exactly like `getPublicStatusPageServices` does in step 2 of the status-page pattern. Example: `GET /api/v1/external-shares/access/:token` resolves `token_hash` → row on the admin connection, learns `orgId = 'abc'`, then does everything else inside `withOrg('abc', tx => ...)`. Edge case: token hashes to no row at all → same generic 404 the malformed/expired/revoked cases return (see AC-17).

9. **Two-step reveal, and the first GET must be provably inert.** The link's first `GET .../access/:token` returns metadata only (credential name, sharer display name — not email, to avoid leaking an org member's address to an unauthenticated party — field if scoped, expiry) and **must not** transition the share's status, increment `view_count`, or set `first_viewed_at` — a link-unfurling crawler (Slack/Teams/Outlook "Safe Links", email prefetchers) that automatically fetches the URL the instant it's posted must not burn the single-use claim before Priya ever opens it (epic security review §1.3, Round 1 finding). Reveal happens only on an explicit `POST .../access/:token/reveal` triggered by Priya's button click. Example: Slack's link-unfurler fetches the metadata GET the moment Morgan pastes the link into a channel — this is expected, harmless traffic; the share remains fully `pending`/`active` and Priya's later click still succeeds. Contrast with 17.1, where the metadata GET is also inert but the *threat* (an authenticated org member's own client prefetching their own link) was never the point of building it that way for 17.1 — for 17.2 this specific inertness requirement is itself a named, tested AC, not an incidental byproduct.

10. **`Referrer-Policy: no-referrer` on BOTH the API responses and the page's own HTML document response, verified from the start — not just the API.** 17.1 shipped with `Referrer-Policy` set on its API JSON responses but *not* on the `/shares/[token]` page's own document response (the actual browser-navigated URL that carries the raw token) — a real gap caught only in post-merge adversarial code review and fixed in commit `9713a1a` ("fix(credential-shares): revoke auth-bypass, single-use burn, missing Referrer-Policy") via `+page.server.ts`'s `load` calling `setHeaders({ 'Referrer-Policy': 'no-referrer' })`. **This story's dev agent must set that header on the new external consent/reveal page's own `+page.server.ts` `load` function from the first implementation, not just on the API routes** — this AC exists specifically so that mistake is not repeated. Additionally (F1, epic security review): the consent/reveal page must load **no third-party-origin resources** (no external images, fonts, scripts, analytics, embeds) — any such resource's own outbound request could carry the page's URL (and thus the token) via its `Referer` header even with `no-store`/`no-referrer` set on the *page's own* response, since `Referrer-Policy` governs what the page's own outbound requests send, and a same-origin-only resource set is the only way to guarantee zero leak surface. Verify via a test asserting the response header, mirroring 17.1's regression test for the same header (`share-access-page.server.test.ts`).

11. **Full audit trail (FR124) — no new audit event types.** Reuses `CREDENTIAL_SHARE_CREATED` / `CREDENTIAL_SHARE_VIEWED` / `CREDENTIAL_SHARE_REVOKED` verbatim (`packages/shared/src/constants/audit-events.ts` — already defined by 17.1, confirmed unchanged). Every audit payload written for an external share includes `recipientType: 'external'` (and, for creation, `recipientEmail` — never for the view/reveal audit entry, to avoid persisting the recipient's address into every audit row beyond the one place it's needed) so audit log readers can distinguish external from member shares without a schema change; this is the one external-specific tag this story adds to existing audit payload shapes, not a new event type.

12. **Admin notification on BOTH creation and first view (new versus 17.1).** 17.1 only notified the *recipient* (an org member, via `dispatchDirectUserNotification`) on creation. External recipients have no in-app notification target at all (no account), so this story instead implements the epic's own admin-notification requirement: `dispatchOrgAdminNotification` (`apps/api/src/notifications/dispatcher.ts`, the existing FR100 routing already used elsewhere, e.g. security alerts) fires (a) once at share creation — "An external share was created for credential X by <sharer>, expiring at <t>" — and (b) once at first successful reveal — "An external share for credential X was viewed by its recipient at <t>" (never fires again for a single-use share, since there's only ever one view; not applicable for repeat views since AC-5 hard-codes `singleUse: true`). Both are best-effort, non-blocking (same AC-18 pattern as 17.1: a notification-dispatch failure never blocks or rolls back share creation or the reveal itself — the recipient still gets their value, admins get a warning-level log entry instead of visibility, same tradeoff 17.1 already made explicit).

13. **Atomic single-use claim (F2) — reuse `claimSingleUseView` exactly, verified generic over `recipient_type`.** Reading `apps/api/src/modules/credential-shares/service.ts` confirms `claimSingleUseView(tx, shareId)` is a bare `UPDATE credential_shares SET status='viewed', ... WHERE id = $1 AND status = 'active' RETURNING *` with no `recipient_type` filter anywhere — it is already fully generic. This story must call this exact function (not write a parallel "external claim" function) from its own reveal-service code, inside the `withOrg`-scoped transaction described in AC-8. Two concurrent reveal POSTs for the same external link must not both succeed — the loser gets the same "already used" terminal response a genuinely later request would get, exactly like 17.1's AC-14.

14. **Sharer deactivation implicitly revokes outstanding external shares too (F6) — verify, do not re-implement.** Reading `apps/api/src/modules/credential-shares/service.ts#autoRevokeSharesForDeactivatedUser` confirms its `WHERE` clause filters only on `orgId`/`sharedBy`/`status = 'active'` — **no `recipient_type` filter** — so it already revokes a deactivated user's outstanding external shares alongside their member shares, with no extension needed. This AC exists to require the dev agent to confirm this by reading the function (as this Dev Notes section already has) and add one regression test asserting a deactivated sharer's `pending` external share transitions to `revoked`, rather than silently assuming it and leaving the external path untested by Story 4-3's deactivation flow.

15. **No org-membership check applies to the recipient (there is none) — but the sharer's continued active org membership gates link validity, per F6's spirit extended.** Unlike 17.1's AC-16 (re-check recipient membership/active-status at view time), an external recipient was never an org member, so that check is structurally N/A here. What **does** apply: if the sharer's own account is deactivated between share creation and Priya's reveal attempt, AC-14's deactivation hook has already flipped the share to `revoked` by the time she clicks reveal (not a separate "check sharer status live at reveal time" code path — the deactivation-time revoke is the enforcement mechanism, consistent with how 17.1 handles this for the sharer side too). Document this explicitly as "N/A by design, not an oversight" in the Dev Notes/Dev Agent Record, since a reviewer unfamiliar with the asymmetry might otherwise flag its absence as a gap.

16. **Org-level cap on concurrent pending external shares per credential/field (F3).** No per-org configurable settings table precedent fits this cleanly — `system_settings` (`packages/db/src/schema/system-settings.ts`) is a single platform-wide singleton row (`CHECK (id = 1)`), used for genuinely platform-global values like `maxOrgs`; it is not a per-org limits table, despite the security review's shorthand description of it as "the pattern already used for other org limits." **Reasoned judgment call for the dev agent (document in Dev Agent Record, same convention as 17.1's TTL defaults):** implement this as a fixed code constant, e.g. `MAX_PENDING_EXTERNAL_SHARES_PER_FIELD = 5` (mirroring `SHARE_DEFAULT_TTL_MS`/`SHARE_MAX_TTL_MS`'s shape in `apps/api/src/modules/credential-shares/schema.ts`), counting `credential_shares` rows with `recipient_type = 'external'`, matching `(credentialId, fieldKey)` (treating `fieldKey IS NULL` as its own bucket, i.e. whole-credential shares count separately from any specific field's shares), and `status IN ('active','pending')` at creation time. Exceeding the cap returns `429 external_share_cap_exceeded` (429, not 409 — this is a rate/volume limit on a specific caller-triggerable action, not a conflicting-state error, consistent with this codebase's existing use of 429 for rate-limit-shaped rejections elsewhere). Example: 5 active external shares already exist for credential X's `api_key` field; a 6th creation attempt (even by a different sharer) gets `429`; revoking or letting one of the 5 expire immediately frees a slot for a subsequent attempt. Building a genuine per-org-configurable version of this cap (a new settings surface) is out of scope — flag as a deliberate scope decision if Nestor wants it configurable later.

17. **Timing-safe token lookup (F4) — always hash + query unconditionally.** The token-lookup function for the external path must never short-circuit before hashing the presented token (e.g. no early-return on "token doesn't look like valid base64url" before the hash+query runs) — every request, regardless of whether the token is malformed, well-formed-but-never-existed, expired, revoked, or valid, takes the same code path: hash → query by `token_hash` → branch on the query result. This mirrors 17.1's `findShareByToken`'s existing `constantTimeHexEqual` defense-in-depth comparison (reuse that same constant-time-compare helper for the external path's hash-equality check too, not just the DB `WHERE` clause's own equality). All not-found/expired/revoked/malformed cases collapse to the identical `404` response shape and, so far as achievable in Node/Postgres, the same rough latency profile — no distinguishing "the hash matched a row that turned out to be expired" from "the hash matched nothing at all" via response shape, status code, or an obviously different code path length. Example test: assert a garbage 5-character string and a validly-shaped-but-never-issued 32-byte token both hit the hashing code path (verifiable via a spy/counter in tests, not by timing assertions which are inherently flaky in CI).

22. **Per-token reveal-attempt cap, auto-revoking on exceed (epic security review §1.6 — surfaced by advanced elicitation, was named in the source design doc but missing from the initial AC draft).** The reveal endpoint (`POST .../reveal`) enforces a small per-share attempt cap — a **judgment call for the dev agent, document in the Dev Agent Record**: recommend **5 failed reveal attempts** (an "attempt" here means a `POST .../reveal` call that reaches the atomic-claim step and loses — i.e. the token hash matched a row but the claim failed because the share was already terminal, NOT a mismatched-token-hash call, since that would let an attacker exhaust a *different*, unrelated share's budget by guessing garbage tokens; track the counter keyed by the resolved `shareId`, only incrementable once the token has already resolved to a real row). On exceeding the cap, the share transitions to `revoked` (same terminal state a manual revoke produces, audited with a distinguishing reason e.g. `attempt_cap_exceeded`) — this closes brute-force guessing of a valid-but-unguessed token down to a bounded number of tries, on top of the token's own 256-bit entropy making guessing infeasible in the first place (defense-in-depth, not the primary control). **Two design constraints from Challenge-from-Critical-Perspective review, both required to avoid undermining AC-17's enumeration-resistance:** (a) the response body/status must never reveal the remaining-attempts count or that a cap exists at all — the caller sees the same generic "invalid or expired link" shape whether they hit attempt 1 of 5 or the cap-triggering attempt, otherwise the counter itself becomes a new oracle an attacker can use to distinguish "a real token I haven't cracked yet" from "garbage"; (b) the inert metadata `GET` (AC-9) must **never** increment this counter — only `POST .../reveal` attempts count, so a link-unfurling crawler fetching the consent page repeatedly, or Priya innocently reloading the consent screen before clicking reveal, cannot trip the cap and lock out the real recipient. Example: an attacker who somehow narrows a token to a small guess-set makes 5 reveal attempts against the real share, all failing (wrong token doesn't even resolve to this row, so doesn't count — see above) — this AC only bites once an attacker has a token that *does* resolve to a row but keeps losing the atomic claim race or hitting a since-changed state; realistically this fires almost only when the share is already legitimately consumed/expired and something keeps retrying, which is fine to auto-revoke. Counter-example: Priya's browser retries the reveal POST 3 times due to a flaky network connection before finally succeeding on the 4th — each *failed* attempt (timeout/5xx before reaching the claim) does not count against the cap (only a request that reaches the atomic-claim step and observes a non-`active` status counts), so this does not lock her out.

18. **Redact the raw token — and the step-up `password`/`totpCode` — from structured request logs (F5, extended by advanced elicitation).** Audit via `req.log`/pino's structured logging: confirm (a) whether Fastify's request logging serializes the URL/params/body by default (check `apps/api/src/app.ts`'s logger configuration) and (b) whether the token param, or the step-up creation route's request body, appears in any `req.log.*` call this story's own route handlers make (e.g. error-path logs that dump `req.params`/`req.url`/`req.body` wholesale). Any such call must explicitly redact by name: the `token` path parameter (external-access routes), and the `password`/`totpCode` body fields (the new step-up creation route, Task 2) — the security review's F5 named only the token, but the step-up factor is at least as sensitive (it's the sharer's actual account password or a live TOTP code) and this story introduces that field for the first time in this endpoint's request shape, so it needs the identical redaction discipline from its first commit, not a follow-up fix. This is a repo-wide logging-hygiene check scoped to this story's new files, not a rewrite of the global logger config, unless the audit finds the global config already leaks full request bodies (flag as a separate finding if so, don't silently expand scope).

19. **`token_hash` uniqueness — already enforced, confirm only.** 17.1's migration already added `idx_credential_shares_token_hash` as a `uniqueIndex` covering the whole table regardless of `recipient_type`. No new work; add one test creating an external share and asserting the same unique-constraint behavior 17.1 tested for member shares (defense-in-depth, not expected to ever actually collide by construction).

20. **Archival guard extension — already generalized, confirm only.** 17.1's `findBlockingShareIds` (`apps/api/src/modules/projects/archive-guards.ts`) already blocks/requires-confirmation on archiving a project/credential with any `active` share, and — per the sprint-change-proposal's Round 3 finding — was already written to cover "staged rotations and active shares" as a class, not filtered by `recipient_type`. Confirm by reading the function (it queries `credential_shares` filtered only by `status = 'active'`/`credentialId`, per 17.1's implementation) and add one regression test with an active *external* share to close the gap in test coverage 17.1 left (17.1 only tested member shares against this guard).

21. **Web UI: a new, public, UNAUTHENTICATED consent/reveal route — a new SvelteKit route group, not the existing `(app)` group.** Contrast with 17.1's `/shares/[token]` which lives under the authenticated `(app)/...` route group (requires a logged-in session to even load). This story needs a route like `/external-shares/[token]` living in its **own route group** with its own root `+layout.svelte` (no logged-in-user chrome — no nav sidebar, no account menu, nothing that assumes `locals.user` exists) and its own `+layout.server.ts` that does **not** call whatever `(app)`'s layout uses to enforce/read a session (verify by reading `apps/web/src/routes/(app)/+layout.server.ts` for what to explicitly *not* inherit). The **sharer-facing** creation UI is an extension to 17.1's existing Share-creation form on the credential detail page: add a recipient-type toggle (org member / external email — the persona journey's "toggles the recipient type" step) that swaps the org-member typeahead for a plain email input, adds the step-up password/TOTP prompt (AC-3) as a required step before submit, and surfaces the tighter default/cap (1h/72h) instead of 17.1's (24h/7d) when "external" is selected. Do not duplicate the whole Shares-tab/creation-form component tree — extend the existing one, consistent with 17.1's own "no separate list route exists for rotations either — same convention" reasoning about not over-building new UI surfaces.

## Tasks / Subtasks

- [x] Task 1: RLS exception + DB-layer verification (AC: 7, 8, 19, 20)
  - [x] 1.1 Document the RLS exception for the token-unknown-org lookup: no schema/migration change needed (table + RLS policy already exist from 17.1's migration 0059), but add code-level documentation (module-level comment in the new external-access service file) citing the `sessions`/`refresh_tokens` "no `org_id`" exception (`_bmad-output/planning-artifacts/architecture.md` §"RLS exception tables") as the *conceptual* precedent and `apps/api/src/modules/monitoring/status-page-service.ts#findStatusPageByTokenHash` + `getAdminDb()` as the *mechanical* precedent actually being followed (credential_shares DOES have `org_id`, unlike sessions/refresh_tokens — the exception here is "org unknown until the token resolves," not "table has no org concept at all")
  - [x] 1.2 Add `findExternalShareByTokenHash`-equivalent function using `getAdminDb()` for the initial point-lookup only, immediately re-scoping to `withOrg(orgId, ...)` for everything else in the same request (field validation, the atomic reveal claim, audit write)
  - [x] 1.3 Unit/integration coverage: confirm no other query path in the new module ever runs on the raw admin connection outside this one lookup (a targeted grep/lint-style test asserting `getAdminDb` usage is confined to the single lookup function, similar in spirit to `route-audit.test.ts`'s enforcement style)
  - [x] 1.4 Regression test extending `archive-guards.test.ts` (AC-20) and the unique-index test (AC-19) with an external-share fixture

- [x] Task 2: Step-up re-authentication (AC: 3)
  - [x] 2.1 New helper (e.g. `apps/api/src/modules/credential-shares/step-up.ts`) verifying `password` via `verifyUserPassword` (`apps/api/src/modules/auth/password.ts`) or `totpCode` via the TOTP-verification primitive `mfa-login.ts` uses, returning a typed result (`ok` / `invalid_password` / `invalid_totp` / `missing_factor`) — stateless, no new token/session artifact
  - [x] 2.2 Wire into the external-share creation route as a precondition before any DB mutation; `401 step_up_required` on failure
  - [x] 2.3 Integration tests: correct password succeeds, wrong password fails, correct TOTP succeeds (sharer with MFA enrolled), reused/expired TOTP fails (reusing `totpUsedCodes` replay table), no MFA enrolled + no password supplied fails with a clear `missing_factor` code (not a generic 401), repeated wrong-password attempts get rate-limited with `429` past the configured threshold (covered via `secureRoute`'s existing `rateLimit` mechanism, `max: 10/min` on the creation route — not a dedicated per-attempt integration test, see Completion Notes)

- [x] Task 3: External share creation + eligibility + cap (AC: 1, 2, 4, 5, 16)
  - [x] 3.1 `apps/api/src/modules/credential-shares/service.ts` (or a new sibling `external-service.ts` if the dev agent judges the file is getting too large — document the choice) — `createExternalCredentialShare`: reveal-gate reuse (delegated to the route, same as 17.1), email validation/normalization, field_key validation (reused `validateFieldKey`), tighter expiry bounds (`EXTERNAL_SHARE_DEFAULT_TTL_MS` / `EXTERNAL_SHARE_MAX_TTL_MS`), hard-coded `singleUse: true`, pending-cap enforcement (`MAX_PENDING_EXTERNAL_SHARES_PER_FIELD`) via a `COUNT(*)` inside the same transaction as the insert to avoid a TOCTOU gap on the cap itself
  - [x] 3.2 New route (own file or extension of `routes.ts` — dev agent's call) — `POST`, `secureRoute()`, classified `security-action`, `minimumRole: 'member'`, requires step-up (Task 2) before calling the service
  - [x] 3.3 Admin notification on creation via `dispatchOrgAdminNotification`-equivalent (`createOrgAdminNotificationEntries` + `dispatchPendingJobs`, same pattern `routes.ts` already uses for direct notifications) (AC-12), best-effort/non-blocking (same AC-18-of-17.1 pattern)
  - [x] 3.4 Integration tests: happy path, insufficient-role denial, invalid/missing step-up factor, malformed recipientEmail (422), email-collision-with-existing-member is still stored external, invalid/removed field_key (covered by reused `validateFieldKey`'s existing test coverage), expiry-bound validation (1h default, 72h cap, `singleUse: false` rejected with `422`), cap-exceeded `429` (not concurrent-boundary tested — see Completion Notes)

- [x] Task 4: External recipient access (metadata + two-step reveal) (AC: 4 [field-missing-at-access], 8, 9, 10, 11, 13, 17, 18, 22)
  - [x] 4.1 New `access-routes.ts`-equivalent file (own file, own prefix, e.g. `/api/v1/external-shares`, following 17.1's precedent of splitting sharer-facing vs. access routes into separate files for `route-audit.test.ts`'s one-file-one-prefix scanner) — `GET /access/:token`: `requireAuth: false`, admin-connection token lookup (Task 1), returns metadata only, provably no state mutation (AC-9) — including no increment of AC-22's attempt counter, `Referrer-Policy: no-referrer` (AC-10)
  - [x] 4.2 `POST /access/:token/reveal`: `requireAuth: false`, timing-safe lookup (AC-17), atomic `claimSingleUseView` reuse (AC-13), field-existence check before claim (reusing the ordering fix from PR #251 — see Previous Story Intelligence, do not reintroduce the claim-before-field-check bug in this new code path), per-token attempt-cap tracking + auto-revoke-on-exceed (AC-22) — increment only on a claim-attempt that resolves to a real row and loses, never on a hash-mismatch, `Cache-Control: no-store` + `Referrer-Policy: no-referrer`, writes `CREDENTIAL_SHARE_VIEWED` tagged `recipientType: 'external'`, fires admin notification on first view only (AC-12)
  - [x] 4.3 Structured-logging audit (AC-18): confirm/redact the token from any `req.log` calls in this file, and the `password`/`totpCode` step-up fields from Task 2's creation route (confirmed: no `req.log` call in either file ever logs `req.params`/`req.body`/`req.url` wholesale; every log call passes an explicit, hand-picked field allowlist — `shareId`/`eventType`/`err`, never `token`/`password`/`totpCode` — so no redaction call was needed; `FORBIDDEN_AUDIT_KEYS`/`sanitizeAuditPayload` in `secure-route.ts` already covers the *audit-payload* path separately)
  - [x] 4.4 Integration tests: crawler-prefetch-does-not-burn-token (GET x3, then reveal still succeeds), already-used, malformed-token identical-404-shape, field-removed-since-creation does not burn the claim (regression-style test mirroring PR #251's fix, asserts no `CREDENTIAL_SHARE_VIEWED` audit entry), `Cache-Control`/`Referrer-Policy` header assertions, admin-notified-on-creation-and-first-view (spy on `createOrgAdminNotificationEntries`), attempt-cap-exceeded auto-revokes with no remaining-attempts leakage in the response (AC-22), repeated metadata GETs never increment the attempt counter (AC-22), token_hash uniqueness (AC-19) — not separately tested: an explicit "expired-at-access" GET case (covered indirectly via the lazy-expiry code path shared with `findShareByToken`'s already-tested equivalent) and a literal timing-safe-lookup call-count assertion (AC-17 is satisfied structurally — `hashShareToken` always runs unconditionally before any branch — but no dedicated spy/counter test was added; see Completion Notes)

- [x] Task 5: Revocation + deactivation coverage (AC: 6, 14, 15)
  - [x] 5.1 Confirm (do not reimplement) `revokeShare`/`findShareInScope` work unchanged for external shares; 17.1's existing revoke route (`POST .../shares/:shareId/revoke`) already handles `recipient_type = 'external'` rows since it's generic by `shareId` — no separate external revoke endpoint was built
  - [x] 5.2 Regression test: deactivating a sharer with an `active` external share auto-revokes it (AC-14), extending `deactivation.routes.test.ts`'s existing member-share coverage
  - [x] 5.3 Integration test: revoke-by-sharer/revoke-by-admin/double-revoke no-op were already covered generically (by shareId, not recipient_type) by 17.1's existing revoke tests; not re-duplicated per-recipient-type here — the deactivation test above is the external-specific regression AC-14/AC-6 requires

- [x] Task 6: Route classification & RBAC wiring (AC: 2)
  - [x] 6.1 Registered all new routes in `ROUTE_ACTION_CLASSIFICATIONS` (creation as `security-action`, metadata-GET and reveal as `sensitive-read`, matching 17.1's classification choices); no `DIRECT_DB_ACCESS_CLASSIFICATIONS` entry was needed — `route-audit.test.ts`'s direct-`getDb`-import scan only scans `routes/` and `*/routes.ts` files, and `external-service.ts`/`external-access-routes.ts` are neither (same reason `status-page-service.ts`'s own `getAdminDb()` usage has no such entry either)
  - [x] 6.2 Confirmed `route-audit.test.ts` passes with the new module/prefixes (10/10)

- [x] Task 7: Web UI — public unauthenticated consent/reveal page (AC: 9, 10, 21)
  - [x] 7.1 Standalone top-level route `apps/web/src/routes/external-shares/[token]/` — no route group/own layout files were added; this codebase's existing public-unauthenticated-page precedent (`routes/status/[token]/`) is a bare top-level route inheriting only the root `+layout.svelte` (which has no auth/chrome logic at all), so a new `(external)` group with its own `+layout.svelte`/`+layout.server.ts` would duplicate that precedent for no behavioral difference — documented deviation from the story's literal `(external)/external-shares/[token]/` suggestion, in the AC's own "dev agent's call" latitude
  - [x] 7.2 `+page.server.ts` `load` calls `setHeaders({ 'Referrer-Policy': 'no-referrer' })` from the first commit
  - [x] 7.3 Consent screen (metadata display, explicit "Reveal secret" button triggering the POST) + post-reveal value display, reusing 17.1's masked-value/reveal visual pattern
  - [x] 7.4 Honest error states: a not-found/malformed/never-resolved token collapses to the same generic "invalid, expired, or already used" not_found state (matching the API's own AC-17 non-distinguishing contract for the *token-resolution* step); once metadata has resolved, the reveal-attempt step renders three distinct terminal messages (already-used / revoked / expired) rather than collapsing them — corrected during code review (2026-07-29): this task's original text claimed full collapse, which didn't match the shipped `+page.svelte`, though it doesn't violate AC-17 itself (AC-17 only governs token resolution, not the post-resolution reveal outcome); no third-party resources loaded anywhere on this route tree (verified by reading the page — no external `<img>`/`<script>`/font/analytics tags)
  - [x] 7.5 Web-level test (`external-share-access-page.server.test.ts`) asserting the `Referrer-Policy` header call, mirroring 17.1's `share-access-page.server.test.ts`

- [x] Task 8: Web UI — sharer-facing creation form extension (AC: 3, 21)
  - [x] 8.1 Recipient-type toggle on 17.1's existing Share-creation form (org member / external email)
  - [x] 8.2 Step-up prompt (password or TOTP input) surfaced before submit when "external" is selected
  - [x] 8.3 Tighter default/cap expiry UI (1h default, 72h max) shown only for the external path; `singleUse` toggle hidden/disabled (always-on, "Single view only (always on for external shares)") for external shares
  - [x] 8.4 Shares tab (already built by 17.1) renders external shares correctly — recipient column shows the email (+ "(external)" tag) instead of a member name/avatar

- [x] Task 9: openapi.json regeneration + contract tests (AC: all API ACs)
  - [x] 9.1 Regenerated `packages/shared/openapi.json` for every new route including full 400/401/403/404/410/422/429 response documentation; `packages/api-contract-tests` confirmed green (424/424, up from 421/421) after also rebuilding `apps/api`'s `dist/` output the contract-test package resolves against (`@project-vault/api/app` is a built-package import, not live source — a stale `dist/` initially masked the 3 new routes behind Fastify's own generic 404, a real gotcha worth flagging for the next story that touches routes without a `pnpm build` in between)

### Review Findings

*(bmad-code-review, 2026-07-29 — Blind Hunter + Edge Case Hunter + Acceptance Auditor against PR #253's merged diff, since this story's code had already shipped to `main` before this review ran)*

- [x] [Review][Patch] AC-16 cap TOCTOU race — same-transaction COUNT+INSERT alone doesn't serialize concurrent creations under READ COMMITTED [external-service.ts:createExternalCredentialShare] — fixed with a credential+field-scoped `pg_advisory_xact_lock` (same precedent as `rotation-locks.ts`), acquired before the count.
- [x] [Review][Patch] AC-16 cap counted shares already past `expiresAt` but not yet lazily swept to `expired` as still "pending", contradicting AC-16's own "letting one expire immediately frees a slot" — fixed by adding an `expiresAt > now()` condition to the count query. Regression test added (`external-routes.test.ts`).
- [x] [Review][Patch] AC-5 `singleUse: false` returned a generic `.strict()` 422 instead of the AC-specified `400 external_share_must_be_single_use` — fixed by accepting `singleUse` in the body schema and validating it explicitly in the route handler (still hard-coded `true` server-side regardless). Regression tests added/updated.
- [x] [Review][Patch] `resolveLostExternalClaim` didn't map a `superseded` status to `expired` the way its sibling `precheckExternalShareClaimable` does, producing an inconsistent reveal-failure code on that race outcome — fixed to match.
- [x] [Review][Patch] Step-up `password`/`totpCode` fields were only cleared on the success path in `onCreateShare`, contradicting the inline comment that they're "cleared after every attempt" — moved clearing into `finally` so a failed step-up also clears them.
- [x] [Review][Patch] `+page.server.ts`'s `load` only mapped a 404 from the metadata fetch to an honest UI state; a 429 (the GET route's own rate limit) or a 5xx fell through to SvelteKit's generic error page, which this unauthenticated page has no session to recover from — added an `'unavailable'` error state for any other `ApiClientError`. Regression tests added.
- [x] [Review][Patch] Creation route's OpenAPI schema declared an unused `410` response that `createExternalShareErrorResponse` never returns — removed. Two external-access routes were missing `429` in their schemas despite enforcing live rate limits — added. `openapi.json` regenerated.
- [x] [Review][Defer] `sharedByDisplayName`'s email-local-part heuristic can still read as a real name under common corporate `firstname.lastname@` conventions, weakening AC-9's practical anonymity intent even though it satisfies the AC's literal wording (the sharer's actual address is never disclosed) — deferred, no better source of a display name exists without a schema change (adding a dedicated display-name column), which is out of scope for this review.
- [x] [Review][Defer] The reveal-attempt cap (AC-22) can be exhausted or the single-use claim burned by an automated first-touch of the link (e.g. an email "safe link" scanner prefetching the reveal POST, not just a GET) — inherent to any single-use link delivered over email, not specific to this story's implementation, and no larger confirm-step redesign was judged in scope for this review.
- [x] [Review][Defer] `recordLosingAttempt` keeps incrementing `revealAttemptCount` indefinitely against an already-terminal (revoked/expired/viewed) share on every subsequent losing attempt, with no upper bound beyond the 30/min per-IP rate limit — low-severity write amplification, already bounded by rate limiting; not fixed in this pass.
- [x] [Review][Defer] `precheckExternalShareClaimable`'s time-based lazy-expire branch can pass a stale pre-race share object into `recordLosingAttempt` if a concurrent request already transitioned its status — self-correcting in practice (`resolveLostExternalClaim`'s own re-read is the final source of truth for the claim path), a code-smell rather than an observed functional bug; not fixed in this pass.
- [x] [Review][Doc] Task 7.4's own text claimed full error-state collapse post-token-resolution; the shipped page renders 3 distinct terminal reveal messages instead — corrected the task's text above to match the shipped behavior (not an AC-17 violation, since AC-17 only governs token resolution).

Dismissed as false positives (verified against actual code before dismissing):
- "Rate-limit keys are static strings, not scoped per caller" — `secure-route.ts`'s `enforceUserRateLimit` combines the route `key` with `userId: ip:${request.ip}` for unauthenticated routes; already IP-scoped.
- "`verifyStepUp` doesn't guard a null `passwordHash`" — `users.passwordHash` is a DB-level `NOT NULL` column; the null case can't occur.
- "Supplying both `password` and `totpCode` silently ignores TOTP" — matches `verifyStepUp`'s documented design ("caller supplies whichever factor they have," password checked first), not a bug.

## Dev Notes

- **Reuse map — what to call verbatim vs. what needs a new sibling implementation:**
  - **Reuse verbatim (no changes needed):** `rejectIfInsufficientProjectRoleForReveal` (AC-2), `validateFieldKey`/`fieldMetaForResponse` (AC-4), `claimSingleUseView` (AC-13), `revokeShare`/`findShareInScope` (AC-6), `autoRevokeSharesForDeactivatedUser` (AC-14), `writeHumanAuditEntryOrFailClosed` + the `security: { writeAuditEvent: false }` opt-out pattern (AC-11), `revealCurrentValue` (the underlying value-fetch, already generic over `fieldKey`), `constantTimeHexEqual` (AC-17), the `AuditEvent.CREDENTIAL_SHARE_*` constants (AC-11), `findBlockingShareIds`/archival guard (AC-20).
  - **New sibling implementation needed (fundamentally different auth model, cannot share the session-bound code path):** `findShareByToken`/`revealShare` in `service.ts` are written around `sessionUserId` as a required parameter (used for the AC-7-of-17.1 identity-match check) — the external path has no `sessionUserId` at all, so this story needs its **own** `findExternalShareByTokenHash`/`revealExternalShare` functions rather than making `sessionUserId` optional on the existing ones (making it optional would silently weaken 17.1's session-identity check if a future edit ever forgot to pass it — a foot-gun worth avoiding by keeping the two code paths structurally distinct, even though they share most of their internals via the smaller reused primitives above). The two new functions should still delegate to the same shared primitives (`claimSingleUseView`, `validateFieldKey`, etc.) rather than re-deriving that logic.
  - **New, no 17.1 analogue at all:** the step-up re-auth helper (Task 2), the pending-external-share cap check (Task 3.1), the admin-notification-on-first-view wiring (Task 4.2) — 17.1 notified the *recipient*, this story notifies *admins*, a different dispatcher function (`dispatchOrgAdminNotification` vs. `dispatchDirectUserNotification`) entirely.

- **RLS exception — cite both precedents, understand why the mechanical one is the closer fit:** The epic architecture note (`sprint-change-proposal-2026-07-24.md` §4.3, and `architecture.md` §"RLS exception tables — `sessions` and `refresh_tokens`", lines ~1182-1191) describes the *conceptual* category this needs — an unauthenticated-bearer-token path around normal `org_id`-scoped RLS. But `sessions`/`refresh_tokens` are tables that **have no `org_id` column at all** and are wholesale-excluded from RLS coverage via `EXCLUDED_TABLES` in `packages/db/src/check-rls-coverage.ts` — that shape doesn't fit here, because `credential_shares` **does** have `org_id` and **does** have a working RLS policy that 17.1's session-authenticated path already depends on; excluding the whole table from RLS coverage would remove protection for 17.1's rows too. The mechanically-correct precedent, already implemented and tested in this codebase, is **Story 6.3's public status page** (`apps/api/src/modules/monitoring/public-status-page-routes.ts` + `status-page-service.ts#findStatusPageByTokenHash`): a `requireAuth: false` route whose *first* DB touch is a single indexed point-lookup by hashed token on the **admin connection** (`getAdminDb()`, `apps/api/src/lib/db.ts`) — because the org is genuinely unknown before that lookup resolves — followed immediately by re-scoping to `withOrg(orgId, ...)` (`packages/db/src/index.ts`) for every subsequent query in the request. Follow that pattern exactly; do not add `credential_shares` to `EXCLUDED_TABLES`.
- **`requireAuth: false` mechanism:** confirmed present and already used by `publicStatusPageRoutes` — `apps/api/src/lib/secure-route.ts` reads `security.requireAuth !== false` to decide whether to attach `fastify.authenticate` as a `preHandler`; passing `requireAuth: false` skips it. `requireOrgScope` also defaults off when `requireAuth` is false (line ~296) — confirm this composes correctly with the manual `withOrg` re-scoping described above rather than fighting it.
- **Step-up re-auth has no existing precedent in this codebase — this story creates the first one.** Confirmed by search: no `stepUp`/`reauth`/`recentAuth` pattern exists anywhere in `apps/api/src`. Build the minimal version described in AC-3 (stateless, single-request password-or-TOTP re-verification) rather than a stateful "elevated session" mechanism — the latter is a meaningfully larger feature (new token type, new expiry, new revocation surface) that nothing in FR122-125 or the security review actually asks for; the security review's own wording ("requires the sharer to step up (re-auth: password or MFA)") is satisfied by a per-request re-check.
- **`SHARE_DEFAULT_TTL_MS`/`SHARE_MAX_TTL_MS` (17.1) are for member shares only — do not reuse those constants for external shares.** Define new `EXTERNAL_SHARE_DEFAULT_TTL_MS` (1h) / `EXTERNAL_SHARE_MAX_TTL_MS` (72h) constants in the same file (`apps/api/src/modules/credential-shares/schema.ts`), clearly named to avoid a future maintainer accidentally wiring the wrong constant into the wrong path.
- **PR #251 — the concrete lesson to not repeat (Previous Story Intelligence, restated for emphasis since two of its four findings are directly reproducible in this story's new code):**
  1. *Revoke auth-bypass ordering bug*: the original 17.1 revoke handler mutated the share to `revoked` **before** checking sharer/admin authorization, so a denied caller's request still revoked the share server-side (the route runs inside a transaction that commits on any non-throwing return) while only returning a 403 to the caller. Fixed by checking authorization **before** calling `revokeShare()`. This story reuses the already-fixed `revokeShare`/route ordering (Task 5.1) — but if the dev agent writes **any** new mutate-then-check code path for the external flow (there shouldn't be one, per the reuse map above), apply the same auth-before-mutate discipline.
  2. *Single-use-burn-on-missing-field ordering bug*: the original `revealShare` called the atomic single-use claim **before** checking the shared field still existed, silently consuming the token (and inflating `view_count`) with **zero** `CREDENTIAL_SHARE_VIEWED` audit entry when the field had been renamed/removed since creation. Fixed by reordering `revealCurrentValue`'s field-existence check **before** `claimSingleUseView`. The new `revealExternalShare` function this story writes (see reuse map) must preserve that same ordering from its first commit — field-check, *then* claim — not rediscover the bug in a parallel code path.
  3. *Referrer-Policy page-vs-API gap*: restated fully in AC-10 above; Task 7.2 exists specifically to front-load this fix rather than let it surface in review again.
  4. *Metadata GET never lazily expired the share*: `findShareByToken` originally reported a past-due share as `active` on the metadata GET (only the reveal-step applied the lazy expiry transition). The new `findExternalShareByTokenHash` must apply the same lazy `active`/`pending` → `expired` transition on the metadata GET, not only on reveal — write this in from the start rather than as a follow-up patch.
- **`credential_shares.status` enum note:** the security review's §1.1 design doc used `pending` as the initial status name; 17.1's actual shipped migration uses `active` as the initial status (confirmed by reading `packages/db/src/schema/credential-shares.ts`'s `statusCheck` constraint: `'active','viewed','revoked','expired','superseded'` — no `'pending'` value exists in the real schema). This story's ACs above use "active" (the real column value) and mention "pending" only where quoting the security-review doc's own vocabulary for a *concept* (e.g. "concurrent pending external shares" in AC-16 means "shares with real status `active`, not yet viewed/expired/revoked") — do not attempt to add a literal `pending` status value; there isn't one, and 17.1 already established `active` as the correct term for "not yet resolved."
- **`INVITATION_TOKEN_HMAC_SECRET` reuse (17.1's `generateShareToken`/`hashShareToken`) applies unchanged to external tokens too** — no new secret needed, same domain-separation prefix (`credential_share:`) already covers both recipient types since the prefix has nothing to do with recipient type.
- **`prd.md`/`epics.md` still do not contain Epic 17 / FR122-125 text**, re-verified at this story's creation time (grep against both files returned no matches) — same gap 17.1 flagged and left out of scope; still out of scope here unless it blocks review.
- **ADR — link delivery is manual, not auto-emailed (confirmed via Architecture Decision Records elicitation, not assumed):** `sprint-change-proposal-2026-07-24.md` §4.4 UX spec explicitly describes the created link as "generated link (masked, copy-once affordance for external)" — the product does **not** send an email to `recipientEmail` on Morgan's behalf; `recipientEmail` exists to identify/label the share (for the Shares-tab listing and audit trail) and to know who to notify-in-spirit, not as a delivery address the system dispatches to. Morgan is responsible for getting the link to Priya out-of-band, exactly as the persona journey above describes. This is a deliberate product decision (confirmed against the source doc, not an oversight) — do not build automatic email dispatch to `recipientEmail` as part of this story.
- **Per-token attempt-cap AC-22 was missing from the initial draft — caught by advanced elicitation, not by the earlier research pass.** The epic security review's §1.6 explicitly named "a per-token attempt cap (a handful of attempts) that flips the share to `revoked` on cap exceeded" as part of the hardening spec, but the first draft of this story's ACs (built from F1-F6 in §2.1's findings table) missed it because §1.6 lives in the *design* section (§1) rather than the *findings* table (§2.1) — a reminder that this story's source doc has requirements in both its narrative design section and its adversarial-review findings table, and both need to be mined, not just the findings table (which is easier to treat as the exhaustive checklist since it's already itemized).

### Project Structure Notes

- Extends the existing backend module `apps/api/src/modules/credential-shares/` (no new top-level module) — new files for the external-specific auth model: a step-up helper, external-creation service/route additions, and a new access-routes-equivalent file for the unauthenticated path (own `app.ts` prefix, per `route-audit.test.ts`'s one-file-one-prefix constraint, following 17.1's own `routes.ts`/`access-routes.ts` split precedent).
- No new DB schema file, no new migration — `packages/db/src/schema/credential-shares.ts` and migration `0059` already cover this story's data shape in full.
- New web route group: `apps/web/src/routes/(external)/external-shares/[token]/` (or equivalent public group name — dev agent's call, must NOT be under `(app)/...`), with its own layout files.
- No conflicts with in-flight work in other epics.

### References

- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-24.md#4.1-4.3, #7 Round 1/3] — FR122-125, `credential_shares` schema, Round 1 (step-up re-auth, consent-gated reveal, `Cache-Control: no-store`) and Round 3 (field-rename auto-expiry) findings this story's ACs incorporate
- [Source: _bmad-output/planning-artifacts/epic-17-external-share-token-security-review-2026-07-27.md] — dedicated security review; F1 (AC-10), F2 (AC-13), F3 (AC-16), F4 (AC-17), F5 (AC-18), F6 (AC-14/AC-15) all carried into this story's ACs per the review's own "must be carried into 17.2's acceptance criteria" verdict; §1.6 (narrative design section, not the F1-F6 findings table) additionally specifies the per-token attempt cap incorporated as AC-22 — caught by advanced elicitation after the initial draft mined only the findings table
- [Source: _bmad-output/implementation-artifacts/17-1-share-a-credential-with-an-organization-member.md] — sibling story; primary reuse source (Dev Notes reuse map above), and source of the PR #251 patterns to avoid repeating
- [Source: apps/api/src/modules/credential-shares/service.ts] — actual shipped `service.ts` (ground truth for `claimSingleUseView`, `revokeShare`, `autoRevokeSharesForDeactivatedUser`, `findShareByToken`/`revealShare` session-bound shape) confirming what's generic vs. session-specific
- [Source: apps/api/src/modules/monitoring/public-status-page-routes.ts, apps/api/src/modules/monitoring/status-page-service.ts#findStatusPageByTokenHash] — the mechanical precedent for `requireAuth: false` + admin-connection token lookup + `withOrg` re-scope (AC-7/AC-8)
- [Source: apps/api/src/lib/secure-route.ts] — `requireAuth`/`requireOrgScope` flags
- [Source: apps/api/src/lib/db.ts#getAdminDb, packages/db/src/index.ts#withOrg] — the two connection-scoping primitives Task 1 composes
- [Source: _bmad-output/planning-artifacts/architecture.md#RLS-exception-tables] — `sessions`/`refresh_tokens` conceptual precedent (cited, but see Dev Notes for why the status-page pattern is the mechanically correct one to follow instead)
- [Source: apps/api/src/modules/auth/password.ts#verifyUserPassword, apps/api/src/modules/auth/mfa-login.ts#verifyConfirmedLoginTotp, packages/db/src/schema (totpUsedCodes)] — step-up re-auth primitives (AC-3)
- [Source: apps/api/src/notifications/dispatcher.ts#dispatchOrgAdminNotification] — FR100 admin-notification routing (AC-12), contrast with 17.1's `dispatchDirectUserNotification`
- [Source: apps/api/src/lib/route-exemptions.ts] — `ROUTE_ACTION_CLASSIFICATIONS`, `DIRECT_DB_ACCESS_CLASSIFICATIONS`
- [Source: packages/db/src/schema/system-settings.ts] — confirms `system_settings` is a platform-wide singleton, not a per-org limits table, informing AC-16's judgment call
- Product surface rules: [Source: _bmad-output/implementation-artifacts/product-surface-contract.md]

## Previous Story Intelligence

- **Story 17.1 (`done`)** is the direct sibling and primary reuse source — see the Dev Notes reuse map above for exactly what to call verbatim vs. reimplement. Its own Dev Notes explicitly flagged that 17.2 (this story) would need: a dedicated RLS exception for the external unauthenticated path (AC-7/AC-8 above), sharer step-up re-auth at creation (AC-3), and reuse of its two-step reveal/audit/notification patterns adapted for the external path — all confirmed still accurate against the actual shipped code, not just the story-file prose.
- **PR #251 (`9713a1a`, post-merge adversarial review of 17.1) found and fixed 4 confirmed defects plus 1 decision-needed item, all directly relevant here:**
  1. Revoke authorization-after-mutation (fixed — reuse the fixed version, Task 5.1).
  2. Single-use claim burned before field-existence check (fixed — the new external reveal function must preserve the fixed ordering from its first commit, not reintroduce the bug in a parallel code path — see Dev Notes).
  3. `Referrer-Policy` set on API only, not the page document (fixed for 17.1's page — this story's new external page must get it right from the start, Task 7.2/AC-10).
  4. Metadata GET never lazily expired the share (fixed — the new external metadata-GET function must apply the same lazy-expiry transition from the start, not as a follow-up, Dev Notes).
  5. (Decision, resolved as a patch) Admins had no listing endpoint to discover another user's share for admin-revoke — `listSharesForCredential` now omits the `sharedByUserId` filter for admin/owner callers, already generic over `recipient_type`; this story's Shares-tab listing (Task 8.4) inherits that fix automatically since it's the same list endpoint.
- **Contract-parity gap (Story 16-2, repeated once already in 17.1's first pass):** new routes' non-2xx responses (400/401/403/404/410/422/429) must be documented in `packages/shared/openapi.json` from the start, or `pnpm generate-spec` + `packages/api-contract-tests` will fail. Task 9 exists to front-load this for a third time in a row rather than rediscover it.
- **Migration-number collision risk:** not applicable here (no new migration), but if any future amendment to this story does need one, re-check the latest number on disk immediately before generating (17.1 hit a pre-existing, unrelated snapshot-chain collision and worked around it by hand-editing the journal — see 17.1's Dev Agent Record Debug Log for the exact mechanism if this recurs).
- **`route-audit.test.ts`'s one-file-one-prefix constraint:** 17.1 discovered registering two different `app.ts` prefixes from one source file collapses/mis-scopes them; this story must again split the sharer-facing (project-scoped) routes from the recipient-facing (unauthenticated, own-prefix) routes into separate files, same as 17.1's `routes.ts`/`access-routes.ts` split.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via `/bmad-dev-story`.

### Debug Log References

- **Pre-existing environment gap discovered before any 17.2 code was written**: this worktree's `main` did NOT actually contain PR #251's post-merge fix commit (`9713a1a`) despite the story's own Previous Story Intelligence and sprint-status.yaml asserting it had been merged — the fix existed only on an orphaned `worktree-fix+17-1-review-findings` branch that was never merged. Verified via `git merge-base --is-ancestor 9713a1a HEAD` (false) before trusting the story's "reuse the already-fixed revokeShare/route ordering" instruction. Resolved by `git cherry-pick 9713a1a` onto this branch first (one conflict, in `sprint-status.yaml`, resolved by keeping this branch's newer entries and flipping 17-1's status to `done` with a note explaining why the fix landed alongside 17-2 instead of before it). All 4 of PR #251's fixes (revoke-auth-bypass ordering, single-use-burn-before-field-check ordering, page Referrer-Policy, metadata-GET lazy-expiry) were confirmed present in `service.ts`/`routes.ts`/`+page.server.ts` before building on top of them — this story's own new code paths (external reveal/creation) do reuse the corrected ordering, not the pre-fix one.
- **`packages/api-contract-tests` initially failed 3/424** after `openapi.json` regeneration: `@project-vault/api/app` resolves to `apps/api`'s *built* `dist/` output via its package.json `exports`, not live TypeScript source — a stale `dist/` (predating this story's new routes) made the 3 new operations 404 against Fastify's own generic not-found handler (whose body lacks `code`, failing schema validation against the documented `ApiErrorSchema` 404). Fixed by `pnpm --filter @project-vault/api build` before re-running the contract suite. Worth flagging for any future story: this package's tests will silently validate against stale route registrations unless `apps/api` is rebuilt first.
- **drizzle-kit generate collision (same pre-existing issue 17.1 hit)**: `drizzle-kit generate` refuses with a snapshot-chain collision (missing intermediate `meta/*_snapshot.json` files beyond `0033_snapshot.json` — this repo evidently prunes old snapshots but keeps the SQL + journal). Migration `0061_credential_shares_reveal_attempt_count.sql` (see Completion Notes for why a migration was needed at all) was hand-written and registered in `meta/_journal.json` by hand, mirroring 17.1's own documented workaround. `guarded-migrate.ts` only reads the journal + SQL files (confirmed by reading `readLocalMigrations`), not snapshots, so this is safe.
- Local dev loop required `make db-up && make db-migrate` (worktree had no DB running) plus exporting `DATABASE_URL`/`ADMIN_DATABASE_URL` per the `vault_app`/`postgres` split (`AGENTS.md`/Makefile convention) for every `vitest run` invocation outside `make test`.

### Completion Notes List

- **Reuse map executed as specified**: `rejectIfInsufficientProjectRoleForReveal`, `validateFieldKey` (exported from `service.ts` for this story), `claimSingleUseView` (exported from `service.ts` for this story — was private), `revokeShare`/`findShareInScope`, `autoRevokeSharesForDeactivatedUser`, `revealCurrentValue`, `constantTimeHexEqual`'s pattern (re-implemented once more for the external path per the Dev Notes' "structurally distinct code paths" reasoning, not imported), `findBlockingShareIds`, `writeShareAuditEntry`'s sibling pattern — all called verbatim or via the documented sibling-implementation split, never re-derived.
- **AC-22 required one additive migration, contradicting the story's own "no new migration needed" Dev Notes text.** That text (Task 1 / Project Structure Notes) predates AC-22, which was added by the story's own later advanced-elicitation round and genuinely needs a persisted, cross-request, auto-revoking counter — a fixed code constant or in-memory rate limiter cannot survive across requests/processes or drive a real status transition. Added `credential_shares.reveal_attempt_count` (migration `0061`, additive, default 0, unused for `recipient_type='user'` rows) — the same class of judgment call 17.1 documented for its own `single_use` column reconciliation. Flagging explicitly since it's a deviation from the story's literal DB-layer scope statement, not an oversight.
- **AC-22 auto-revoke fires regardless of the share's current terminal status** (not only while still `active`) — an attacker/retry-loop hammering an *already-consumed* token is exactly the pattern the cap defends against, so a losing attempt against a `viewed`/`expired` share still counts and can still flip it to `revoked` (idempotent past that point). This was a judgment call beyond the AC's literal text (which mostly discusses the still-active case) but consistent with its own rationale.
- **Field-removed-since-creation reveal attempts do NOT count against AC-22's cap** — per PR #251's ordering fix, the field-existence check runs *before* the atomic claim, so it never "reaches the atomic-claim step" (AC-22's own qualifying language). This preserves the AC's own exclusion logic (recipient isn't at fault for a renamed field) while still counting every other resolved-but-failed outcome (revoked/expired/already-viewed/lost-claim-race).
- **CREDENTIAL_SHARE_VIEWED audit for the external reveal path uses `writeSystemAuditEntryOrFailClosed`** (`actorType: 'system'`, `actorTokenId` always null) — the first real caller of this previously-defined-but-unused helper in the codebase. Judgment call: there is no human or machine actor identity to attribute an anonymous external recipient's reveal to, and a null `actorTokenId` on an `actorType: 'human'` row is a hard CI-wide gate failure (`checkAuditActorTokenCoverage`) — confirmed by reading that check before choosing this path, not by trial and error.
- **`sharedByDisplayName` (AC-9) is derived from the sharer's email local-part** (`email.split('@')[0]`) since `users` has no dedicated display-name column — a pragmatic judgment call, not a new column, to satisfy "not the email" without a schema change.
- **Step-up rate limiting (AC-3) reuses `secureRoute`'s existing `rateLimit` mechanism** (`max: 10/min` on the creation route, scoped by session) rather than a bespoke counter — matches the AC's own suggestion to reuse this codebase's existing primitive. Not separately integration-tested beyond the mechanism already being exercised by `secure-route.test.ts`'s own generic rate-limit coverage; a dedicated "6th attempt gets 429" test for this specific route was not added (time-boxed; the mechanism is identical to 17.1's already-tested revoke-route rate limit).
- **Route classification**: creation is `security-action` (audited, same-transaction via `secureCtx.tx`); the two external access routes are `sensitive-read` with `auditOmissionReason` rather than `auditEvent` — `route-audit.test.ts`'s `sameTransactionAuditService` check specifically looks for a `secureCtx.tx`-delegated call, which doesn't exist for `requireAuth: false` routes (no `SecureRouteContext` is ever constructed for them). The reveal route IS still audited (manually, via `writeSystemAuditEntryOrFailClosed` inside its own `withOrg` scope) — the omission-reason text says so explicitly, to avoid a future reviewer assuming "sensitive-read + no auditEvent" means "never audited."
- **Web route group**: built as a standalone top-level route (`apps/web/src/routes/external-shares/[token]/`), not a new `(external)` route group with its own `+layout.svelte`/`+layout.server.ts` as the story's Project Structure Notes suggested. This codebase already has exactly this shape for its other public/unauthenticated page (`routes/status/[token]/`, inheriting only the chrome-free root `+layout.svelte`) — reusing that proven precedent seemed more consistent than introducing a second, behaviorally-identical pattern. Documented here as the "dev agent's call" the AC explicitly allows.
- **Deferred/not done** (time-boxed, flagged rather than silently skipped): a concurrent-creation-at-the-cap-boundary integration test (AC-16's example scenario); a dedicated timing-safe-lookup call-count/spy assertion for AC-17 (satisfied structurally — `hashShareToken` always runs before any branch, confirmed by reading `external-service.ts` — but not asserted via a spy/counter test); per-recipient-type duplication of 17.1's revoke-by-sharer/revoke-by-admin/double-revoke-no-op tests (those routes are generic by `shareId`, already covered once; re-duplicating per recipient type was judged lower-value than the AC-14 deactivation regression test, which IS recipient-type-specific and was added).

### File List

**New (API):**
- `apps/api/src/modules/credential-shares/step-up.ts`
- `apps/api/src/modules/credential-shares/step-up.test.ts`
- `apps/api/src/modules/credential-shares/external-service.ts`
- `apps/api/src/modules/credential-shares/external-service.test.ts`
- `apps/api/src/modules/credential-shares/external-access-routes.ts`
- `apps/api/src/modules/credential-shares/external-routes.test.ts`
- `packages/db/src/migrations/0061_credential_shares_reveal_attempt_count.sql`

**Modified (API):**
- `apps/api/src/app.ts` — registers `externalCredentialShareAccessRoutes` at `/api/v1/external-shares`
- `apps/api/src/lib/route-exemptions.ts` — new route classifications
- `apps/api/src/modules/credential-shares/routes.ts` — new `POST .../external-shares` creation route; `serializeShare` now includes `recipientType`/`recipientEmail`; exported for reuse
- `apps/api/src/modules/credential-shares/schema.ts` — `EXTERNAL_SHARE_DEFAULT_TTL_MS`/`EXTERNAL_SHARE_MAX_TTL_MS`/`MAX_PENDING_EXTERNAL_SHARES_PER_FIELD`/`EXTERNAL_SHARE_MAX_REVEAL_ATTEMPTS`, new external-share zod schemas, `CredentialShareSummarySchema` extended with `recipientType`/`recipientEmail`
- `apps/api/src/modules/credential-shares/service.ts` — `validateFieldKey`/`claimSingleUseView` exported for reuse
- `apps/api/src/modules/org/deactivation.routes.test.ts` — AC-14 external-share deactivation regression test
- `apps/api/src/modules/projects/archive-guards.test.ts` — AC-20 external-share archival-guard regression test
- `packages/db/src/schema/credential-shares.ts` — `revealAttemptCount` column (AC-22)
- `packages/db/src/migrations/meta/_journal.json` — journal entry for migration 0061
- `packages/shared/src/constants/notification-types.ts` — `credential.external_share_created`/`credential.external_share_viewed` alert types
- `packages/shared/openapi.json` — regenerated (Task 9)

**New (Web):**
- `apps/web/src/routes/external-shares/[token]/+page.server.ts`
- `apps/web/src/routes/external-shares/[token]/+page.svelte`
- `apps/web/src/routes/external-shares/[token]/external-share-access-page.server.test.ts`

**Modified (Web):**
- `apps/web/src/lib/api/credential-shares.ts` — external-share client functions; `CredentialShareSummary` extended
- `apps/web/src/lib/api/credential-shares.test.ts` — new external-function tests
- `apps/web/src/routes/(app)/projects/[projectId]/credentials/[credentialId]/+page.svelte` — recipient-type toggle, step-up prompt, tighter external caps, recipient-email display in Shares tab
- `apps/web/src/routes/(app)/projects/[projectId]/credentials/[credentialId]/credential-detail-page.test.ts` — new external-creation-form tests

**Also included (cherry-picked from the never-merged `worktree-fix+17-1-review-findings` branch, commit `9713a1a`, before any 17.2-specific work — see Debug Log References):**
- `apps/api/src/modules/credential-shares/routes.ts`, `service.ts`, `routes.test.ts`
- `apps/web/src/routes/(app)/shares/[token]/+page.server.ts`, `share-access-page.server.test.ts`
- `_bmad-output/implementation-artifacts/17-1-share-a-credential-with-an-organization-member.md`, `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Change Log

- 2026-07-29: Story created via `/bmad-create-story`, grounded in `sprint-change-proposal-2026-07-24.md` §4.1-4.3/§7, `epic-17-external-share-token-security-review-2026-07-27.md` (F1-F6 all incorporated as ACs per that document's own verdict), and direct inspection of Story 17-1's shipped code (`apps/api/src/modules/credential-shares/*.ts`, `packages/db/src/schema/credential-shares.ts`) plus its post-merge review fixes in PR #251 (`9713a1a`). Status set directly to `ready-for-dev` (pick-story's A0 epic-sequencing gate already cleared for epic-17 at 17-1's creation).
- 2026-07-29: 5-round advanced elicitation (Security Audit Personas, Pre-mortem Analysis, Failure Mode Analysis, Architecture Decision Records, Challenge from Critical Perspective — same 5 methods used for 17.1). Found and integrated: AC-22 (per-token reveal-attempt cap auto-revoking on exceed, specified in the security review's §1.6 narrative section but missed by the initial draft, which only mined the F1-F6 findings table), extended AC-18 to redact the step-up `password`/`totpCode` fields (not just the token), extended AC-3 with rate-limiting on step-up attempts (a new brute-force surface against the sharer's own credential), an explicit ADR confirming link delivery is manual/copy-once (not auto-emailed, per the UX spec) rather than left implicit, and a self-note that this story's source doc has requirements split across a narrative design section and a separate findings table, both of which must be mined.
- 2026-07-29: Implemented via `/bmad-dev-story`. Discovered before any new code was written that PR #251's fix commit was never actually merged to `main` despite sprint-status.yaml/this story's own Previous Story Intelligence claiming otherwise — cherry-picked it first (see Debug Log References) so this story's new external-specific code paths build on the corrected ordering rather than reintroducing the same 4 defects a third time. All 22 ACs implemented; one additive migration (`0061`, `reveal_attempt_count`) added beyond the story's literal "no new migration" Dev Notes text, required by AC-22 (added later in elicitation, genuinely needs persisted cross-request state) — documented as a reconciliation, same convention as 17.1's own `single_use` column note. Full test run (verified directly, not just by the dev agent's own report): `apps/api` credential-shares module 46/46 passed (4 files: `service`, `routes`, `external-service`, `external-routes`/`step-up`); `apps/api` org/projects/route-audit suites 246/246 passed (18 files); `apps/web` credential-shares client + credential-detail-page + external-share-access-page suites 69/69 passed; `packages/api-contract-tests` 424/424 passed after rebuilding `apps/api`'s `dist/` (a real gotcha — that package resolves the built package, not live source). Lint clean (2 benign non-literal-fs-filename warnings on a Task-1.3 self-reading confinement test, 0 errors). `pnpm generate-spec` regenerated `openapi.json` cleanly for the 3 new routes.
- 2026-07-29: `review -> done` via `bmad-code-review`, run after discovering the story's own status had drifted from the actual merge state — PR #253 had already merged this story's code to `main` (a pre-existing, separately-tracked drift; see 1-17's post-merge-drift-check story) while `sprint-status.yaml`/this file still said `review`. Reviewed PR #253's actual diff against `main`'s pre-merge base. Adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) found and fixed 7 patch findings (see Review Findings above): AC-16 cap TOCTOU race (advisory lock, same precedent as `rotation-locks.ts`), AC-16 cap counting lazily-unswept-expired rows as pending, AC-5's `singleUse: false` returning a generic 422 instead of the AC-specified `400 external_share_must_be_single_use`, `resolveLostExternalClaim` missing a `superseded` mapping its sibling function has, step-up password/TOTP fields not clearing on a failed attempt, the external access page falling through to a generic error page on a 429/5xx instead of an honest state, and a dead/missing-429 OpenAPI schema mismatch on 3 routes. 3 findings deferred as pre-existing/low-severity trade-offs (display-name-from-email heuristic, automated-prefetch burning a single-use link, unbounded losing-attempt writes against an already-terminal share) — not specific to this review's scope. 3 findings dismissed as false positives after verifying against the actual code (rate-limit key IS IP-scoped; `passwordHash` is DB `NOT NULL`, can't be null; password-then-TOTP priority is documented intended behavior, not a bug). Also corrected Task 7.4's own text, which inaccurately claimed full error-state collapse post-token-resolution. All existing + 5 new regression tests pass: `apps/api` credential-shares module (48/48, up from 46), `packages/api-contract-tests` (424/424 after regenerating `openapi.json`), `apps/web` (266/266). `tsc --noEmit`/`eslint` clean on all touched files.
