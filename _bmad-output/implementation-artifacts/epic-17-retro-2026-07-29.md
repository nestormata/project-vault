# Epic 17 Retrospective — Secret Sharing & Exposure Tracking

**Date:** 2026-07-29
**Facilitator:** Bob (Scrum Master), via `my-epic-retro` + `bmad-retrospective`
**Mode:** Run autonomously — Nestor explicitly requested this retro be completed without him
("I can't attend the retro, do it without me"). Every point where the workflow normally halts for
{user_name} input was instead answered in the voice of a reasonable, evidence-grounded team lead,
and is marked below as an autonomous call. Nothing here should be read as Nestor's actual words.

---

## Epic 17 Summary

**Secret Sharing & Exposure Tracking** — users can temporarily share a credential's value, in full
or by field, with an org member or an external recipient via an expiring tokenized link, with full
share history and a rotation-recommended nudge afterward. Registered 2026-07-24 via
`bmad-correct-course` (`sprint-change-proposal-2026-07-24.md`, item 3), new FR122–FR125. Depends on
Epic 13's `field_key` model; independent of Epic 14/15/16.

| Metric | Value |
|---|---|
| Stories completed | 3/3 (17-1, 17-2, 17-3) — 100% |
| Prior retros for this epic | None — first retrospective |
| Security gate | Dedicated design-level security review required before story creation began (`epic-17-external-share-token-security-review-2026-07-27.md`), same rigor as Epic 14's SSO work — cleared 2026-07-27, F1–F6 required as ACs on 17-2 |
| Adversarial review coverage | 3/3 stories reviewed (Blind Hunter + Edge Case Hunter + Acceptance Auditor); 17-1: 4 patch findings fixed; 17-2: 7 patch findings fixed + 3 deferred + 3 dismissed false-positive; 17-3: 2 HIGH findings fixed + 6 deferred |
| New migrations | 0059 (credential_shares), 0061 (reveal_attempt_count), 0062 (credential_share_nudge_dismissals) — all 3 hit the project's pre-existing drizzle-kit snapshot-chain collision and required hand-written journal registration |
| CI health | All 3 stories landed with fully green suites (see each story's own record for exact counts — e.g. 17-3: 430 contract tests, 230 credential-shares/rotation/workers/route-audit tests, full apps/web suite, all green) |
| Status-header drift | 17-2 drifted (`review` in sprint-status/story file while PR #253 had already merged to `main`) — caught and fixed by PR #258 via `bmad-code-review`, using the drift-detection mechanism 1-17 built specifically for this class of gap |
| Live verification | 17-3 manually verified end-to-end in a live Chrome session (external share creation with step-up re-auth, status filter across all 5 statuses, reason-required nudge dismissal persisted server-side, rotation-promote superseding a share with an audit entry) |
| Gap & Risk Audit (this retro) | 0 Critical, 3 High, 1 Medium, 0 Low |

---

## Step 6: Epic Review Discussion

### What went well

Bob (Scrum Master): "Let's start with the good stuff. What went well in Epic 17?"

Charlie (Senior Dev): "The reuse discipline was excellent. 17-2 didn't reinvent 17-1's reveal gate,
field_key model, revoke path, audit events, or single-use claim logic — it reused
`claimSingleUseView`, `revokeShare`, `findShareInScope`, and `autoRevokeSharesForDeactivatedUser`
verbatim. Same story for 17-3 reusing `validateFieldKey` from `service.ts` rather than
reimplementing it in `external-service.ts`. Three stories, one shared foundation, no copy-paste."

Dana (QA Engineer): "And the security-first process was real, not theater. A dedicated design-level
security review ran *before* any story creation began — F1 through F6 came out of that review, and
I went back and checked: all six were actually implemented in the shipped code, not just claimed.
F2's TOCTOU race and F3's stale-count bug were even caught and fixed by the *code review*, on top of
the design review already having flagged the general risk area. That's defense in depth working as
intended."

Alice (Product Owner): "From a product angle, 17-3's proactive consolidation stood out to me —
instead of writing a third inline `active -> expired` transition (there were already two: creation
lazy-expire and reveal lazy-expire), the team refactored to one shared
`lazilyExpireShareIfDue(...writeSystemAuditEntryOrFailClosed...)` function and pointed all three call
sites at it. That's exactly the kind of debt-avoidance instinct we want, and it happened without
anyone asking for it."

Elena (Junior Dev): "The rotation-supersession loop closing between Epic 17 and Story 5.6 (staged
rotation) was satisfying to watch land — `supersedeOutstandingSharesForRotation()` wired into the
same transaction as `promoteRotation()`, confirmed acyclic. That was flagged as a gap in the
original sprint-change-proposal's Round 5 elicitation and it actually got built, not just noted."

Bob (Scrum Master): "Nestor, what stood out to you as going well in this epic?"

**Nestor (Project Lead) — autonomous answer, evidence-grounded:** "Two things. First, the
adversarial-review discipline caught real, exploitable bugs before they reached anyone outside the
review process — 17-3's cross-user information disclosure on `GET .../nudge` (any project member
could read another user's `mostRecentSharedWith` recipient email) is the kind of bug that's easy to
miss and expensive to have found in production; it was caught by two independent review layers
(Blind Hunter and Edge Case Hunter) raising it separately, which is a good sign the process isn't
just rubber-stamping. Second, the willingness to fix status drift transparently rather than paper
over it — PR #258 didn't just flip 17-2's status, it actually re-reviewed PR #253's merged diff from
scratch and found 7 more patch-level findings that a rubber-stamp status flip would have missed."

Alice (Product Owner): "That's a good callout — the drift wasn't treated as a formality to clear,
it became a second real review pass."

### Challenges

Bob (Scrum Master): "Now the harder half. Where did we struggle?"

Elena (Junior Dev): "The migration numbering collision bit us three separate times in this one
epic — 17-1's 0059, then 17-2's 0060 collided with 16-4's own 0060 and had to be renumbered to
0061, then 17-3 hit the *same* 0060 number again and had to go to 0062. Every time it's a manual
catch-and-renumber, and every time it's luck that whoever's implementing notices before merge."

Charlie (Senior Dev): _mildly defensive_ "To be fair, that's not an Epic 17 problem — Story 13-1
already flagged 'a pre-existing drizzle-kit generate snapshot-chain collision predating this
branch,' and Epic 15's retro called it out as 'a recurring friction point' after Story 3.5/3.6 hit
it too. This is the fourth and fifth time now and nobody's actually fixed the underlying tooling."

Dana (QA Engineer): "Which is sort of the point, though — we keep writing it down as a one-off
annoyance in each story's dev notes instead of treating it as the same recurring defect. Five
occurrences and it's never once been converted into an action item with an owner."

Bob (Scrum Master): "Fair. We'll make sure that doesn't happen a sixth time — more on that in the
Gap & Risk Audit."

Elena (Junior Dev): "The other thing — 17-2 discovered mid-story that PR #251 (17-1's own
post-merge fix) hadn't actually been merged to `main` despite records claiming it was. That's a
process integrity gap, not a code gap, but it meant 17-2 had to cherry-pick 17-1's fix before
building on top of it, and then deal with a rebase once #251 did land mid-flight."

Bob (Scrum Master): "Nestor, what's your read on the migration-collision pattern and the merge-state
confusion?"

**Nestor (Project Lead) — autonomous answer:** "Both are systemic, not this epic's fault
specifically — but Epic 17 is where the migration-collision pattern crossed from 'annoying, noted
in passing' to 'happened three times in one epic, on top of two before it.' That's the trigger point
for actually fixing it rather than complaining about it again next epic. The merge-state confusion
is different — 1-17 (post-merge-drift-check) exists specifically because this project caught a
similar gap before, and it's reassuring that the *tooling* caught 17-2's own drift automatically
this time (that's exactly why 1-17 was built), even though the underlying PR #251 confusion had to
be caught by a human noticing during 17-2's own implementation."

Bob (Scrum Master): "Speaking of patterns, I noticed something when reviewing all the story
records and cross-referencing every prior retro's action items — three real findings came out of
that pass, and I want to walk through them before we close out the epic review."

*(See the Gap & Risk Audit below for the full detail behind this beat — the three High findings and
one Medium finding were mined directly from this cross-file consistency and follow-through pass, not
invented for the ceremony.)*

Dana (QA Engineer): "Oh — the deferred-work.md gap is the one that stings a little. Epic 16's own
retro literally said, in its Key Takeaways, that self-flagged deferrals go untracked as easily as
any other kind of finding. And then Epic 17 — the very next epic — did exactly that with all 10 of
its own deferred code-review findings."

Charlie (Senior Dev): "That's... actually embarrassing, yeah. Though in our defense, the lesson was
about *tracking discipline*, not something a developer implementing 17-2/17-3 would have had reason
to think about mid-story — it's squarely a retro/audit-level catch, not a dev-story-level one."

Bob (Scrum Master): "No shame in it — that's precisely why the audit exists. Nestor, did you notice
these patterns during the epic?"

**Nestor (Project Lead) — autonomous answer:** "Not in real time, no — and that's exactly the
argument for running this audit at every retro rather than trusting story-level self-discipline
alone. I'd rather this get caught here, one epic late, than not at all."

### Previous retrospective (Epic 16) follow-through

Bob (Scrum Master): "Before we move on, let's circle back to Epic 16's retrospective, since that's
the epic directly before this one."

Bob (Scrum Master): "Epic 16's retro closed with three Key Takeaways. Takeaway 1 was just
'shipped clean, well-tested, security-conscious work' — not an action item, nothing to check.
Takeaway 2 was that the sprint-status rollup-key drift needed to stop being 'track and hope' and
become an enforced gate — that CI guard (`check-sprint-status-rollup.ts`) was built in that same
session, and I confirmed it's wired into `make ci` and GitHub Actions today, and Epic 17's own
rollup key never drifted once across all three stories. That lesson was applied successfully."

Alice (Product Owner): "And Takeaway 3?"

Bob (Scrum Master): "Takeaway 3 is the one we just discussed — 'self-flagged deferrals inside story
docs are exactly as likely to go untracked as any other kind of finding.' Epic 17 repeated that
exact pattern with its own 10 deferred findings across 17-2/17-3. That lesson was *not* applied,
even though it was written down one epic earlier."

Charlie (Senior Dev): "Worth being honest about why, though — Takeaway 3 was phrased as an
observation, not a concrete process change with an owner and an artifact. It's a good instinct for
next time: turn 'here's a pattern we noticed' into something mechanically enforced, not just
narrated."

Bob (Scrum Master): "Agreed, and that's now itself an action item — see below."

### Summary

**Successes:** strong code-reuse discipline across all three stories; a security-first process that
held up under later verification (F1–F6 substantively implemented, not just claimed); proactive
technical-debt avoidance (consolidating the `active -> expired` transition rather than writing a
third copy); two independent review layers catching a real cross-user information-disclosure bug.

**Challenges:** a migration-numbering collision that has now recurred five times project-wide with
zero fix attempts; a merge-state confusion (PR #251) that required a manual catch mid-story; Epic
16's own most-recent lesson about deferred-item tracking discipline recurring one epic later.

**Key insights:** the project's CI-guard-building track record (rollup drift, story-status-sync,
psc-tbd-tracking, story-references) is strong when a pattern gets a concrete artifact attached to
it, and weak when it stays narrative. Both of this retro's process-carryover findings (migration
collisions, deferred-work backfill) fit that same story — they were *noticed* before, just never
converted into something enforceable.

---

## Step 7: Next Epic Preparation

Bob (Scrum Master): "Epic 18 doesn't exist yet in `epics.md` or `sprint-status.yaml` — there's
nothing planned past Epic 17 in the Phase 2 lineup right now."

Alice (Product Owner): "So there's no next-epic dependency check to run today."

Bob (Scrum Master): "Correct — skipping straight to action items, per the workflow's own fallback
for when there's no next epic defined."

---

## Step 8: Action Items

### Process Improvements

1. **Enforce the migration-numbering-collision fix, not just note it again.**
   Owner: Dev team (via `1-18-fix-drizzle-migration-snapshot-chain-collision`, scheduled this
   session).
   Success criteria: either the `meta/` snapshot chain is repaired so `drizzle-kit generate` works
   unaided, or a CI check exists that fails a PR whose new migration number collides with one
   already merged to `main` since the branch was cut.

2. **Backfill deferred-work.md discipline explicitly, don't rely on memory of Epic 16's lesson.**
   Owner: Whoever runs the next epic's retro/audit.
   Success criteria: every `[Review][Defer]` finding in a story's own Review Findings section has a
   matching `deferred-work.md` entry by the time that epic's retro closes — checked explicitly, not
   assumed.

### Technical Debt

- TD17-1 through TD17-10 (`deferred-work.md`, "Deferred from: Epic 17 retrospective") — backfilled
  this session from 17-2/17-3's own Review Findings sections. All Low/Medium, self-corrects or
  bounded by existing rate limiting; no immediate action required beyond the tracked entries.
- 1-18-fix-drizzle-migration-snapshot-chain-collision — scheduled as a real backlog story (not
  debt-only), given five documented occurrences project-wide and zero fix attempts.

### Documentation

1. epics.md — Epic 17 section added (title, FR coverage, security-review pointer; full ACs
   intentionally left in the story files themselves rather than reconstructed after the fact — see
   Finding 1 below for the reasoning).
   Owner: fixed directly this session.
2. prd.md — FR122–FR125 added under a new "Secret Sharing & Exposure Tracking" heading; FR/area
   counts corrected (95 → 99 FRs, 12 → 13 areas).
   Owner: fixed directly this session.

### Team Agreements

- A narrative "Key Takeaway" in a retro doc is not the same as an enforced gate — if a pattern is
  worth naming twice, it's worth converting into a concrete artifact (a script, a CI check, a
  checklist line) the first time it's named a second time, not the third.
- `deferred-work.md` backfill is now an explicit item to check at the start of every future retro's
  Gap & Risk Audit, not something to trust from memory of "we said this before."

### Significant Discovery Check

Bob (Scrum Master): "Did anything from Epic 17 fundamentally change our understanding of a future
epic's plan?"

Charlie (Senior Dev): "No — there's no Epic 18 yet, and nothing here invalidates any existing
epic's design. The security-review-before-story-creation pattern used here is worth reusing
verbatim for any future unauthenticated-access-surface work, but that's a process recommendation,
not a correction to existing plans."

**Epic Update Required: NO.** No significant discoveries requiring a plan revision for any existing
or future epic.

---

## Step 9: Critical Readiness Exploration

Bob (Scrum Master): "Epic 17 is marked complete in sprint-status, but is it *really* done? Let's
walk the readiness checklist."

**Testing & Quality:** All three stories carry adversarial-review coverage (Blind Hunter + Edge Case
Hunter + Acceptance Auditor) with real findings fixed, not rubber-stamped — 17-1: 4 patch findings;
17-2: 7 patch findings + 3 correctly-deferred + 3 correctly-dismissed false positives; 17-3: 2 HIGH
findings fixed same-day. Full regression suites green across `apps/api`, `packages/api-contract-tests`,
and `apps/web` per each story's own record. 17-3 additionally received live-browser Chrome
verification end-to-end. **No open test gaps identified.**

**Deployment:** All three stories merged to `main` (PR #251, #253, #258, #255). 17-2's status drift
(sprint-status/story-file said `review` while the code was already live on `main`) is resolved —
`main` and the tracking files agree today. **Live.**

**Stakeholder Acceptance:** No external stakeholder rejection or pending feedback recorded in any
story file. This is an internal/self-hosted product feature with no separate external-stakeholder
sign-off gate documented elsewhere in this project's process — **treated as accepted by default**,
consistent with how other epics in this project have closed.

**Technical Health:** Sound. RLS confirmed present on both new tables (`credential_shares`,
`credential_share_nudge_dismissals`) via their migration files; RBAC uses the `minimumRole`
convention consistently across all new routes; no copy-pasted validation logic found across the
three stories (shared helpers reused, one deliberate non-reuse documented and reasoned). The three
High findings from this retro's audit are gaps in *tracking and documentation*, not defects in what
shipped.

**G2 Gate (Product Surface Contract):** Both sides of both sharing flows have real, reachable UI —
sharer-side share creation is inline in the existing credential-detail page; recipient-side reveal
is a real standalone route (`apps/web/src/routes/external-shares/[token]/`), not API-only. **G2
satisfied**, no open Critical product-surface gap.

**Unresolved Blockers:** None blocking. The migration-collision pattern is real, recurring debt, but
it has never once blocked a merge — it's caught and hand-fixed every time before landing. Scheduled
for a proper fix (1-18) rather than left to recur a sixth time.

**Epic 17 status: complete from a story, security, and technical-health perspective.** This retro's
findings are gaps in what got tracked and documented after the fact (epics.md/prd.md, deferred-work.md,
the migration-collision pattern itself), not defects in what shipped.

---

## Step 10: Closure

Bob (Scrum Master): "Epic 17: Secret Sharing & Exposure Tracking — REVIEWED."

**Key Takeaways:**

1. A security-review-gate-before-story-creation process, run once for a genuinely higher-risk
   surface (the project's first unauthenticated-access-to-secret-material pathway), held up under
   later independent verification — worth reusing verbatim for the next similarly-risky surface.
2. Five project-wide occurrences of the same un-fixed migration-numbering collision is the clearest
   signal yet that "track and hope" has run its course for that specific pattern — same conclusion
   Epic 16's retro reached about the sprint-status rollup drift, one epic before it actually got
   fixed.
3. A narrative "Key Takeaway" from one retro is not self-enforcing in the next epic — Epic 16's own
   lesson about deferred-item tracking recurred immediately in Epic 17, because naming a pattern and
   converting it into an enforced check are two different, both-necessary steps.

**Commitments made today:** 2 process improvements, 10 technical-debt entries backfilled, 1 new
backlog story scheduled, 2 documentation fixes applied directly, 1 mechanical security fix applied
directly.

**Nestor (Project Lead) — autonomous closing note:** "Good session. Nothing here changes my
confidence that Epic 17 shipped soundly — the findings are entirely about tightening the tracking
and documentation discipline around future epics, which is exactly what this kind of audit is for."

---

## Gaps, Inconsistencies & Risks Audit

*(requested focus: incongruencies, inconsistencies, gaps, technical debt, and risks)*

### 1. [High] `epics.md` and `prd.md` never received the Epic 17 / FR122–125 entries `sprint-change-proposal-2026-07-24.md` specified verbatim

`sprint-change-proposal-2026-07-24.md` §4.1 states "New FR group needed for sharing (proposed
FR122–FR125, next available after FR121)" and §4.2 provides the exact "New Epic 17" section text
(lines 217–248 of that proposal) intended for insertion into `epics.md`. Neither ever happened:
`grep -c "FR122\|FR123\|FR124\|FR125" epics.md prd.md` returned `0` for both files as of this retro,
despite all three Epic 17 stories being `done` and the epic itself rolled up to `done` in
`sprint-status.yaml`. This is not the same as the accepted "ad-hoc epic, no epics.md entry"
precedent used for Epic 11/12/16-3 (those never had planned text to insert) — this proposal
literally drafted the section and it was silently never applied, while sibling Phase-2 epics 13–16
(registered the same way, via the same `bmad-correct-course`/`bmad-sprint-planning` process) all do
have their `epics.md` entries.

**Resolution:** Fixed directly this session — autonomous call, since this is low-risk, mechanical
documentation work, not a design decision. `epics.md` gained a right-sized Epic 17 section (title,
FR coverage, security-review pointer, Release Scope Summary row) rather than a full
Given/When/Then reconstruction of all three stories' ACs after the fact — deliberately, to avoid
fabricating AC text that might not byte-for-byte match what actually shipped; the entry instead
points to the three story files as the authoritative AC source. `prd.md` gained FR122–FR125 under a
new "Secret Sharing & Exposure Tracking" heading, with the FR/capability-area count corrected
(95 → 99 FRs, 12 → 13 areas).

### 2. [High] 17-2/17-3's own code-review deferred findings were never backfilled into `deferred-work.md` — recurring the exact pattern Epic 16's retro named one epic earlier

17-2's Review Findings section lists 3 `[Review][Defer]` items (display-name-from-email heuristic,
single-use link burnable by automated prefetch, unbounded `recordLosingAttempt` writes) plus 2 more
lower-severity deferred code-smell notes; 17-3's lists 6 `[Review][Defer]` items (pagination
tiebreaker, stale nudge display, lazy-vs-sweep stale-read race, page-bookmark UI gap, hardcoded page
size + no double-submit guard, AC-8 doc-placement gap). None of these 10 items appear anywhere in
`_bmad-output/implementation-artifacts/deferred-work.md` (confirmed via grep — zero hits for
"17-1", "17-2", "17-3", "credential_share", or "Epic 17" before this session). This is the identical
failure mode `epic-16-retro-2026-07-28.md`'s Key Takeaway 3 named explicitly: "Self-flagged
deferrals inside story docs ('flagged for a reviewer,' 'not built here') are exactly as likely to go
untracked as any other kind of finding." That lesson was written down one epic before this one
repeated it — not tagged `[REPEAT]` under this skill's strict rule (Key Takeaway 3 was narrative
observation, not a formal action item with a named concrete artifact), but a real, immediate
recurrence worth calling out plainly rather than letting the pattern go silent a second time.

**Resolution:** Fixed directly this session — autonomous call, mechanical backfill with no code
risk. Added a new "Deferred from: Epic 17 retrospective (2026-07-29)" section to `deferred-work.md`
with TD17-1 through TD17-10, each citing its source story file and a stated trigger-to-revisit,
following this project's existing per-epic section format exactly.

### 3. [High] Drizzle-kit migration snapshot-chain collision recurred 3 more times within Epic 17 alone — 5 occurrences project-wide, zero fix attempts, never tracked

Story 13-1's own dev notes first documented "a pre-existing drizzle-kit generate snapshot-chain
collision predating this branch." `epic-15-retro-2026-07-27.md` named it again narratively
("Migration numbering collided a second time... the same class of collision previously seen on
Story 3.5/3.6 (0043 → 0045)... a recurring friction point of this project's hand-written-migration
convention") but never converted it into a formal action item or scheduled a fix. Epic 17 then hit
it three more times in one epic: 17-1's migration 0059 (hand-registered after a collision in that
worktree), 17-2's 0060 colliding with Story 16-4's own 0060 (renumbered to 0061), and 17-3's 0060
colliding a second time with the same 16-4 number (renumbered to 0062). That's 5 documented
occurrences across this project's history, with a hand-catch-and-renumber workaround every single
time and no fix ever attempted at the tooling level, and no `deferred-work.md` entry recording any
of it until this session.

**Resolution:** Scheduled as a new backlog story — autonomous call, preferring a real tracked story
over debt-only given how many times this has now recurred with zero remediation attempts. Added
`1-18-fix-drizzle-migration-snapshot-chain-collision: backlog` under `epic-1` (this project's
established home for cross-cutting infra/tooling stories — 1-13 through 1-17 all live there), with
`epic-1` reopened `done` → `in-progress` following the same "completion round N" convention used
for 2-9/4-5/5-6/6-5/9-7/13-4/1-16/1-17. Scope note in the story entry: either repair the `meta/`
snapshot chain so `drizzle-kit generate` works unaided again, or (lower-risk, faster) add a
migration-number-collision CI check.

### 4. [Medium] F5 security-review finding partially unmet: `totpCode` missing from the durable log-redaction registry despite being an explicit AC

The pre-shipping security review's F5 finding required "any structured request-logging/audit
middleware applied to share endpoints must redact the token path parameter/body field by name, not
rely on 'we don't log secret values' alone." 17-2's step-up body carries the field `totpCode`
(`apps/api/src/modules/credential-shares/schema.ts:90`), but `apps/api/src/lib/redact-paths.ts`'s
`BODY_SENSITIVE_LOG_FIELDS` only listed `totp`, not `totpCode` — a near-miss that would have left a
future logging change that dumps `req.body` wholesale on the step-up path leaking a live TOTP code.
No live leak exists today (confirmed via direct review of every `req.log` call in
`external-access-routes.ts` and `routes.ts` — none currently dumps the body wholesale), but the
durable, registry-level safeguard the AC actually asked for was missing.

**Resolution:** Fixed directly this session — mechanical, one-line, zero behavioral risk. Added
`'totpCode'` to `BODY_SENSITIVE_LOG_FIELDS` in `apps/api/src/lib/redact-paths.ts`, which
automatically covers both the Pino redact-path mechanism and the `redactBodyForLog`/
`REDACTED_BODY_FIELDS` manual-redaction path (they share the same source array). Verified via
`redact-paths.test.ts` (2/2 passing, unchanged assertions now covering the new field).

### Process Pattern Carryover (Step 1.5 evidence base)

This retro relied on `epic-16-retro-2026-07-28.md`'s own comprehensive follow-through table as the
most recent baseline (it confirmed `check-story-status-sync`, `check-sprint-status-rollup`,
`check-psc-tbd-tracking`, and `check-story-references` all built and wired into `make ci`, and both
of that retro's own open items — the rollup-key drift `[REPEAT 3x]` and the
`architecture.md` secrets/credentials rename `[REPEAT 4x]` — were closed in that same session).
Re-verified clean here: `check-sprint-status-rollup`, `check-story-status-sync`,
`check-story-references`, and `check-psc-tbd-tracking` all pass on this branch today, and no new
occurrence of either of those two prior repeat-patterns appears in Epic 17's own stories.

**No `[REPEAT]`-tagged findings this round.** Findings 1–3 above are genuine recurrences of
previously-*narrated* patterns (Epic 15's migration-collision note, Epic 16's deferred-work
Key Takeaway), but neither was ever logged as a formal action item with an id in a prior retro's
Action Items table — the strict `[REPEAT Nx]` rule (recurs from a prior retro's *action item*) does
not apply to either, though both are called out explicitly above as immediate, real recurrences
rather than being quietly treated as new.

**Reverse-sequencing guard (Step 1.4):** At the start of this retro, every epic 1 through 16 was
`done` in `sprint-status.yaml` (confirmed via a full scan of every `epic-N:` key) — no earlier epic
was in-progress. `epic-1`'s reopening to `in-progress` for `1-18` is this retro's own output, not a
pre-existing condition the guard needed to flag.

**Security-review follow-through (F1–F6):** All six pre-shipping security-review findings were
re-verified against the actual shipped code, not just the story files' claims, per this project's
repeated lesson that "claimed resolved" and "actually built" have diverged before (e.g. Epic 15's
A12-3). F1 (Referrer-Policy + no third-party resources), F2 (atomic advisory-lock-guarded single-use
claim), F3 (org-level pending-share cap), F4 (timing-safe token lookup), and F6 (deactivation
auto-revoke) are all substantively implemented and covered by regression tests. F5 (log redaction)
was partially met — see Finding 4 above, now fully closed.

No other findings rose to Critical/High/Medium in the requirement/UI-coverage or project-invariant
categories — RLS coverage, RBAC `minimumRole` convention, and reachable UI on both sides of both
sharing flows all came back clean with direct code evidence (see this retro's research notes:
`0059_credential_shares.sql:38-40`, `0062_credential_share_nudge_dismissals.sql:26-28`,
`apps/web/src/routes/external-shares/[token]/`).
