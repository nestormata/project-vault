# Epic 16 Retrospective (Round 2): Custom Theming — Completion Round Closure

**Date:** 2026-07-29
**Facilitator:** Bob (Scrum Master)
**Participants:** Bob (Scrum Master), Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev), Nestor (Project Lead)
**Mode:** Run autonomously via `my-epic-retro` in worktree `chore/epic-16-retro`, per Nestor's explicit request ("I can't attend the retro, do it without me") to re-review Epic 16 now that its two completion-round follow-ups (16-3, 16-4) have shipped since the first retro. No live turn-by-turn participation — every HALT and roleplay beat below proceeds by making the most reasonable, evidence-grounded call and recording both the call and the fact that it was made autonomously.

**Why a second retro for the same epic:** `epic-16-retro-2026-07-28.md` ran when Epic 16 had 2/2 original stories done, found two real gaps, and scheduled `16-3` and `16-4` to close them, reopening `epic-16` to `in-progress` to hold for that work. Both stories have since shipped (`16-3`: done 2026-07-28; `16-4`: done 2026-07-29) and `epic-16` is back to `done`. This is precisely the "epic kept moving after its retro" case this skill exists to catch — the follow-up stories themselves need their own review, not an assumption that the first retro's routing was the end of the story.

---

## Epic 16 Summary (updated)

**Epic 16: Custom Theming** — administrators can brand a self-hosted instance with a custom theme, and now can also trigger a reload from the UI and configure an org-wide default that applies before a user has personalized anything.

Delivery Metrics:
- Completed: 4/4 stories (100%) — 16-1, 16-2 (round 1, retro'd 2026-07-28), 16-3, 16-4 (round 2, this retro)
- Blockers encountered: 0 net-new (16-4's `make ci` attempt 1 failed on a pre-existing unrelated flaky test — `org-sso-domains-routes.test.ts` AC-4 — confirmed not touched by this story; attempt 2 was clean)
- Production incidents: 0

Quality and Technical (16-3 / 16-4):
- Both went through `bmad-code-review`'s adversarial triage (Blind Hunter / Edge Case Hunter / Acceptance Auditor) post-implementation, not just pre-implementation elicitation.
- 16-4 found and fixed one real High finding at review time: Task 6.3's "reload-race regression" test didn't actually race a concurrent reload — fixed (commit `cf22ed2`) by racing the domain-lookup request against a real concurrent `reloadThemes()` call.
- 16-3 surfaced one Medium finding left as documented, accepted debt: `invalidateAll()` shares a try/catch with `triggerThemeReload()`, so a post-success `invalidateAll()` rejection could misattribute an error/MFA banner over an otherwise-successful reload. Low exploitability, no fix applied — this is a properly-tracked debt item (named, in the story's own record), not a hidden gap, so this retro does not re-flag it as a new finding.
- Both were manually verified live against a running `make docker-up` stack in Chrome, continuing the pattern Elena praised in the first retro.

Business Outcomes:
- FR120/FR121 (theme install/selection) plus this round's two follow-ups: an admin can now trigger a theme reload from `(app)/settings/themes/` without curl, and an OrgAdmin can configure an org-wide default/fallback theme applied to pre-auth login-screen branding (SSO-domain-mapped orgs only) and to any authenticated user who hasn't personalized their own theme yet.
- 16-2's self-flagged org-wide-vs-per-user conflict (Finding 4 of the first retro) is now substantively resolved by 16-4, not just "confirmed" — per-user selection still wins when set; an org default fills the gap underneath it.

---

## Team Discussion

**Bob (Scrum Master):** "Round two. Last time we found two real gaps and scheduled fixes — let's see how they landed and whether anything new showed up."

**Charlie (Senior Dev):** "16-3 is clean — mirrors the `triggerBackup`/`canManageSsoDomains` patterns exactly, no new backend surface, and it actually caught and fixed something along the way: `architecture.md`'s Requirements-to-Structure table still said `(app)/admin/themes/` was 'unconfirmed' — Task 5 corrected it to the real `(app)/settings/themes/` path. That's a nice small instance of the cross-file-consistency category actually getting self-corrected during implementation instead of needing a retro to catch it."

**Dana (QA Engineer):** "16-4 is the more interesting one. It didn't just build the org-default column — it ran its own Round 5 stakeholder round table to force a final decision on its *own* two flagged judgment calls (pre-auth scope boundary, the NULL-collapse trade-off) before implementation started. That's exactly the discipline the first retro's Key Takeaway #3 was asking for: don't let a self-flagged item just sit as prose."

**Alice (Product Owner):** "Except — and I want to flag this clearly — it still landed with two *new* self-flagged Out-of-Scope items of its own: pre-auth branding on `register`/`invitations` pages, and the bigger 'zero-input pre-auth branding' gap. Both are reasoned through, both have a stated justification for not building them now, but neither has a backlog entry or a `deferred-work.md` line. I checked — zero hits for either in the backlog or `deferred-work.md`."

**Bob (Scrum Master):** "Right, that's the finding I want the team to sit with. The first retro's Key Takeaway #3 said, verbatim, that self-flagged deferrals in story docs are exactly as likely to go untracked as any other kind of finding. The very next story built off that retro's own routing repeated the pattern — with better internal reasoning than 16.2's original conflict, but still no forcing function converting it into tracked work. That's a second occurrence of the same defect class, one story later. This project's other repeat-finding guards treat a second occurrence as the trigger point, not a third — I'm applying that same bar here even though this isn't a formally Step-1.5-tracked action item with a named artifact."

**Elena (Junior Dev):** "So it's not a `[REPEAT Nx]` in the strict sense — no prior retro logged 'artifact asked for: X, check if X exists' — but it's the same shape of problem showing up twice in a row."

**Charlie (Senior Dev):** "Which argues for treating it with the same seriousness the formal REPEAT rule gets: don't let it become a third occurrence in whatever Epic 16's next touch-point is. Schedule the two concrete gaps now, and see if we can put a guard in front of the general pattern so it stops depending on a retro noticing it."

**Bob (Scrum Master):** "Agreed — I'm routing this one under the REPEAT restrictions (schedule and/or guard, not dismiss or debt-only) even though it isn't formally REPEAT-tagged from Step 1.5's table. See Action Items."

**Nestor (Project Lead):** [Autonomous session — no live response captured. Consistent with his participation pattern across the Epic 14/15/16 rounds, the team proceeds treating "does this convert into tracked work, not just prose" as the standing question guiding every routing decision below, and defaults to scheduling real backlog entries and guards over dismissing findings when no one is present to weigh in live.]

**Dana (QA Engineer):** "One more small thing worth naming and then closing: 16-4's NULL-collapse trade-off (`users.selected_theme_name = NULL` meaning both 'never chosen' and 'explicitly chose Default') was explicitly considered and explicitly rejected as its own follow-up (`theme_selection_is_explicit` flag) for lack of evidence it matters in practice. That one I'd treat differently from the pre-auth gaps — it's genuinely speculative, not a known gap, so tracked debt without a story feels proportionate."

**Bob (Scrum Master):** "Agreed, that one doesn't need the same treatment as the REPEAT-adjacent finding."

### Reverse-Sequencing Guard Check (Step 1.4)

**Bob (Scrum Master):** "Before we go further — checked every epic numbered below 16 in `sprint-status.yaml`. All of epic-1 through epic-15 are `done`. No lower-numbered epic is mid-flight while we retro Epic 16. Clean, no warning to carry forward this time."

### Previous Retrospective Follow-Through (Step 1.5 evidence table)

Built fresh this session rather than trusting the first retro's own characterization at face value, per this skill's own instruction to verify the artifact, not the claimed resolution:

| Action item (from `epic-16-retro-2026-07-28.md`) | Artifact asked for | Found on disk today? | Result |
|---|---|---|---|
| Finding 1 `[REPEAT 3x]` rollup drift → build CI guard | `scripts/check-sprint-status-rollup.ts` wired into `make ci` / CI workflow | Yes — file exists, `Makefile:122` and `.github/workflows/ci.yml:108` both call it | **Resolved and holding.** `epic-16`'s rollup key is `done` today, correctly matching 16-1..16-4 all `done`. Bonus evidence the guard class works in practice: commit `293d3d6` ("fix(sprint): sync story 16-3 Status header with sprint-status.yaml") shows the sibling `check-story-status-sync` guard catching and fixing a real header/status-key drift on 16-3 during this very round. |
| Finding 2 `[REPEAT 4x]` secrets→credentials rename → verify/fix | `docs/architecture.md` terminology | Re-checked: still consistently uses "secret" for the generic crypto concept and "credential" for the domain entity | **Still holding** — no regression since the first retro's "already resolved" verification. |
| Finding 3 `[High]` admin UI for theme reload → schedule story | `16-3-admin-ui-trigger-for-theme-reload` in `sprint-status.yaml` | `done`, PR #250 merged (`e1005d1`) | **Resolved.** UI now exists at `(app)/settings/themes/`, consuming the existing endpoint, no new backend surface — matches the finding's scoped ask exactly. |
| Finding 4 `[Medium-High]` org-wide vs. per-user conflict → schedule story | `16-4-org-wide-default-theme-for-pre-auth-and-new-users` in `sprint-status.yaml` | `done`, PR #252 merged (`c69f24b`) | **Resolved**, and resolved *well* — per-user selection confirmed as non-negotiable, org default added as an additive fallback layer, exactly the shape 16.2's own text anticipated. |
| Finding 5 `[Low]` `allowedRoles` → `minimumRole` | `apps/api/src/modules/theming/routes.ts` reload route | Confirmed `minimumRole: 'admin'` in the route definition | **Resolved and holding.** |
| Sibling guards (not Epic-16-specific, checked for regression): `check-story-status-sync`, `check-psc-tbd-tracking`, `check-story-references` | All three scripts + `make ci`/CI wiring | All three exist and are wired (`Makefile:121,123,124`; `ci.yml:105,111,114`) | **Holding.** |

All five of the first retro's action items independently verified on disk, not just claimed. This is a clean follow-through round — the only new material is the self-flagged-deferral recurrence discussed above, which is a new observation about 16-4's own text, not a failure to close out anything from round 1.

---

## Next Epic Update: Epic 17 (Secret Sharing & Exposure Tracking)

**Bob (Scrum Master):** "Round 1 previewed Epic 17 as 'next in the backlog, not started.' That's stale now — it's well underway: `17-1` (share with an org member) is `done`, `17-3` (share history, expiry enforcement, rotation nudge) is `done`, and `17-2` (share with an external recipient via a secure link) is in `review` on its own in-flight worktree, untouched by this session."

**Charlie (Senior Dev):** "Nothing in this round's findings bears on Epic 17's design — theming and secret-sharing remain independent modules, no shared backend surface either direction, same as round 1 concluded."

**Alice (Product Owner):** "Worth flagging procedurally, not as a defect: Epic 17 started and has nearly finished while Epic 16 was still mid-completion-round (`16-3`/`16-4` in flight). That's the same shape as the reverse-sequencing pattern this skill's Step 1.4 watches for, but it doesn't trigger the guard here, because Epic 16's *retrospective itself* had already run (round 1) before Epic 17 started — the guard exists to stop a later epic's retro outrunning an earlier epic's unstarted retro, not to stop parallel development once a retro has already happened once. No action needed, just noting the distinction for the record."

---

## Readiness Assessment

- **Testing & Quality:** 16-3: 9 new component tests + 4 server tests, full `apps/web` suite green (216 files / 1771 tests), `tsc`/`eslint`/`prettier` clean. 16-4: `make ci` green on attempt 2 (266/266 API test files across all packages), attempt 1's single failure confirmed pre-existing and unrelated. No open test gaps.
- **Deployment:** Both merged via PR with passing CI (16-3: PR #250; 16-4: PR #252). No pending deployment blockers.
- **Stakeholder Acceptance:** No pending stakeholder feedback identified in either story's record; both were live-verified in Chrome against a running stack.
- **Technical Health:** Sound. 16-3 adds zero new backend surface (pure UI consuming an existing endpoint). 16-4 adds one nullable column (migration 0060, no CHECK constraint — deliberately, since valid values are dynamic/filesystem-defined and validated at request time), one new admin-only PATCH route following the existing `organization-settings-routes.ts` pattern (minimumRole, requireMfa, fail-closed audit, rate-limited), and extends 14.4's pre-auth domain-lookup endpoint rather than inventing a new one.
- **Unresolved Blockers:** None. `epic-16` is `done`, not held open for anything further from this round — the two new backlog items below (16-5, 16-6) are follow-ups, not blockers of anything.

**Epic 16 status: complete, both delivery rounds closed out.** The one new finding below is again a gap in what got tracked, not a defect in what shipped — consistent with round 1's framing.

---

## Action Items — Final Routing (Step 4, post-ceremony)

1. **Finding A — `[Treated as REPEAT 2x]` self-flagged deferrals recur untracked (pre-auth branding gaps in 16-4):** Nestor's autonomous-session default per this skill's guidance ("prefer scheduling real backlog entries or tooling guards over dismissing") — **both** schedule and guard:
   - Added `16-5-pre-auth-branding-for-registration-and-invitation-pages: backlog` to `sprint-status.yaml` under Epic 16 — the smaller, clearly-scoped gap (extend the existing two-step domain-lookup pre-auth branding mechanism, already used by the login page, to `(auth)/register` and `(auth)/invitations/*`).
   - Added `16-6-anonymous-pre-auth-org-branding-resolution: backlog` — the larger gap (branding before any email is typed at all), explicitly flagged in 16-4's own Out of Scope section as needing a new general-purpose pre-auth org-resolution primitive; scoping that primitive is `plan-story 16-6`'s job, not this retro's.
   - Attempted to encode the general pattern as a guard in `pick-story`'s Path A (new step "A2.6: Self-Flagged Deferral Disposition Check", to run after elicitation and before the story-creation commit, requiring every "Out of Scope"/self-flagged-deferral bullet in a new story to carry either a linked backlog entry or an explicit reasoned dismissal before it lands). **This edit was blocked by the session's own permission classifier** (writes to `~/.claude/skills/*`, outside this repository, are restricted from this worktree session) — logged as **deferred, owner: Nestor**, full proposed text captured below under "Proposed skill-guard text (blocked, for manual application)" so it isn't lost as unconverted prose either.
   Status: **Partially done this session (2 backlog entries scheduled) + 1 deferred (skill edit blocked by permission scope, text preserved for manual application).**

2. **Finding B — `[Low]` NULL-collapse distinguishing flag (`theme_selection_is_explicit`) considered and rejected in 16-4:** Not treated as REPEAT (first time raised, and already reasoned through in-story via a stakeholder round table with a stated "no evidence it matters in practice" justification). Nestor's autonomous default: **tracked debt only, no story** — recorded here, no `sprint-status.yaml` change.
   Status: **Debt tracked in this doc; revisit trigger = a real user complaint or support ticket about being unable to force the base theme despite an org default being configured.**

`epic-16` remains `done` — this round found nothing that requires reopening it; both new backlog entries are independent follow-up features, not corrections to anything that shipped.

### Proposed skill-guard text (blocked, for manual application)

Intended insertion point: `~/.claude/skills/pick-story/SKILL.md`, as a new step between `A2.5` and `A3`.

```markdown
### A2.6: Self-Flagged Deferral Disposition Check (untracked-prose guard)

**Why:** This project's retros have repeatedly found that a deferral written into a story's own
text — "Out of Scope," "flagged for a reviewer to confirm," "a plausible future story, not built
here" — has close to a 0% historical conversion rate into tracked work unless something forces the
conversion. `epic-16-retro-2026-07-28.md` named this explicitly as Key Takeaway #3 after finding two
such cases (16.1's admin-UI reload gap, 16.2's org-wide-vs-per-user conflict) sitting untouched with
no backlog entry. The very next story built from that retro's own routing — 16-4 — repeated the
identical pattern in its own text (two new self-flagged Out-of-Scope items) with, again, no backlog
entry or `deferred-work.md` line — see `epic-16-retro-2026-07-29.md`. That's a second occurrence of
the same defect class one story after the first was named.

**Action, after A2's elicitation rounds are integrated and before A3 commits the story:**
1. Grep the newly created/updated story file for deferral language: `Out of Scope` section bullets,
   and phrases like "flagged for", "not built here", "future story", "future follow-up", "reviewer to
   confirm or challenge".
2. For each hit, check whether it already has a clear disposition: a linked existing
   `sprint-status.yaml` backlog entry, or an explicit, reasoned "no follow-up needed" statement in
   the story's own Dev Notes (e.g. a stakeholder round table that considered and rejected it).
3. Any hit with neither must not be left as bare prose: add a new `backlog` entry for it now, or ask
   the user explicitly if scope is too ambiguous to guess.
4. Advisory-but-mandatory: must run and its outcome must be recorded, but should not indefinitely
   block A3 if the user explicitly wants to defer disposition — in that case, note in the
   sprint-status.yaml comment that the deferral is a known, named gap rather than an invisible one.
```

---

## Key Takeaways

1. Both completion-round stories (16-3, 16-4) shipped clean, well-tested, and — notably — 16-4 actually practiced the discipline round 1 asked for (forcing its own flagged judgment calls to a stakeholder decision before implementation) rather than just re-flagging them.
2. Despite that discipline, 16-4 still produced two new self-flagged deferrals with no tracked follow-up — confirming Key Takeaway #3 from round 1 wasn't a one-off observation but a real recurring failure mode that needed a second data point to justify treating it at REPEAT-level urgency.
3. The project's CI-guard track record continues to hold up under direct verification, not just self-report: `check-sprint-status-rollup` and `check-story-status-sync` are both confirmed catching real drift in commits from this very round (`293d3d6`), not just sitting unused.
4. A skill-guard fix attempted in this session was blocked by the harness's own permission boundary (this session, being scoped to a project worktree, cannot write to global `~/.claude/skills/*`) — worth noting as a process constraint for future autonomous retro rounds: skill-file edits proposed under Step 4 option 5 may need to be applied by a session with broader write scope, or drafted here for the user to paste in, as done above.

---

## Gaps, Inconsistencies & Risks Audit

*(requested focus: incongruencies, inconsistencies, gaps, technical debt, and risks)*

### 1. [High, treated as REPEAT 2x] Self-flagged story deferrals continue to land with no tracked follow-up

`16-4-org-wide-default-theme-for-pre-auth-and-new-users.md:140-143` (Out of Scope section) contains two explicitly self-flagged, reasoned deferrals — "Zero-input pre-auth branding" (no mechanism exists to resolve org identity for a truly anonymous request) and "Pre-auth branding on `(auth)/register` or `(auth)/invitations/*`" (only the login page's flow is extended) — each stated as "a plausible follow-up, not built here" / "not silently forgotten." A grep of `sprint-status.yaml` for any backlog entry mentioning "register", "invitation", "pre-auth", or "branding" under Epic 16, and a grep of `deferred-work.md` for "theme", both return zero hits. This is the identical defect class `epic-16-retro-2026-07-28.md`'s Key Takeaway #3 named one retro ago ("self-flagged deferrals inside story docs are exactly as likely to go untracked as any other kind of finding") — recurring in the very next story built from that retro's own routing. It is not a formally Step-1.5-tracked `[REPEAT Nx]` item (no prior retro logged a specific named artifact to check for), but this project's own established bar for repeat findings is "treat a 2nd occurrence as the trigger point, don't wait for a 3rd" — applying that same bar here rather than waiting for a third occurrence to "earn" the same seriousness.

**Resolution:** Routed per REPEAT-equivalent restrictions (schedule and/or guard, not dismiss/debt-only). Scheduled as two new backlog entries (`16-5-pre-auth-branding-for-registration-and-invitation-pages`, `16-6-anonymous-pre-auth-org-branding-resolution`) and as an attempted `pick-story` skill guard (blocked by this session's write-scope restriction to global `~/.claude/skills/*`; full proposed text preserved above for manual application).

### 2. [Low] NULL-collapse distinguishing flag left as speculative, unbuilt follow-up

`16-4-org-wide-default-theme-for-pre-auth-and-new-users.md:260` documents "Option B — add a distinguishing flag (rejected for this story)": a new `users.theme_selection_is_explicit` column would let a user force "always show base, even if my org sets a default," rejected as "speculative scope — no evidence yet that this distinction matters in practice." This is a first-time-raised deferral, not a recurrence, and it already carries an explicit reasoned rejection (not bare prose) from the story's own Round 5 stakeholder round table.

**Resolution:** Tracked as debt only, no story — proportionate given the explicit "no evidence it matters" justification. Revisit trigger: a real user report of needing to force the base theme despite an org default.

### 3. [Informational] Cross-file consistency, RBAC/security invariants, and process-guard health all checked clean

- `Status:` headers for both `16-3-admin-ui-trigger-for-theme-reload.md` and `16-4-org-wide-default-theme-for-pre-auth-and-new-users.md` (`done`) match their `sprint-status.yaml` entries exactly.
- `architecture.md:1148`'s Theming Requirements-to-Structure row was corrected during 16-3's own implementation (Task 5) to reflect the real `(app)/settings/themes/` path rather than the stale "unconfirmed `(app)/admin/themes/`" note — a self-caught fix, not something this retro needed to find.
- `epic-16`'s rollup key (`sprint-status.yaml:317`) is `done`, correctly matching all four child stories and the retrospective key — no drift this round, and the `check-sprint-status-rollup`/`check-story-status-sync` CI guards are independently confirmed still wired and, per commit `293d3d6`, actively catching real drift during this very completion round.
- 16-4's new PATCH route (`/default-theme-settings`) follows the established `minimumRole: 'admin'`, `requireMfa`, fail-closed-audit, rate-limited (`10/60s`) convention consistently with the rest of `organization-settings-routes.ts` — no RBAC or audit-completeness invariant violations found.
- Reverse-sequencing guard (Step 1.4): no epic numbered below 16 is non-`done` — clean, nothing to carry forward this round.

No further Critical/High findings surfaced in requirement/UI coverage, technical debt/duplication, or project-invariant categories beyond Finding 1 above — 16-3 and 16-4's own adversarial code-review passes (Blind Hunter/Edge Case Hunter/Acceptance Auditor) already surfaced and disposed of their own Medium/Low debt (16-3's `invalidateAll()`/`triggerThemeReload()` shared-try/catch misattribution risk; 16-4's pre-existing NULL-collapse and theme-name-enumeration patterns), and this retro does not re-flag debt that is already properly named and tracked in-story.
