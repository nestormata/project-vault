# Epic 15 Retrospective — Round 2 (Closure Verification)

**Date:** 2026-07-28
**Epic:** 15 — Localization
**Mode:** Run autonomously per Nestor's explicit request (chained task: `bmad-code-review 14-8` →
`my-epic-retro epic 14` → `my-epic-retro epic 15` → gap stories → rebase/push/PR), no live
participation.

## Executive Summary

This is the **second** retrospective pass on Epic 15. The first (`epic-15-retro-2026-07-27.md`)
found the cleanest audit result in this project's history (0 Critical/High), with only 1 Medium
(settings-PATCH handler duplication, tracked as debt) and 2 already-documented Low items, plus a
same-session post-ceremony fix of two cross-epic items (P5-1, A12-3).

**This round's Gap & Risk Audit found 1 Critical finding**: `epic-15`'s own rollup key in
`sprint-status.yaml` was still `in-progress` even though both of its stories (15-1, 15-2) and its
retrospective have all been `done` since 2026-07-27 — the identical rollup-drift pattern Epic 14's
own first/second retros caught and fixed for themselves (`epic-14-retro-2026-07-26.md` Finding 1,
`epic-14-retro-2026-07-27.md` Finding 1). **Fixed directly this session** (`epic-15: in-progress ->
done`), rather than scheduled as a story — this is exactly the class of drift this project's retros
fix in-place per established precedent (epic-1, epic-2, epic-4, epic-6, epic-14 all had the same
kind of rollup-key fix applied directly during their own retros). No other new findings.

## Ceremony (condensed — autonomous run, no live participant)

### Step 1: Epic Confirmation

Epic 15 confirmed per explicit user argument ("epic 15"). Both stories (15-1, 15-2) done, matching
`epics.md`'s full FR117-119 scope for this epic (confirmed no third story is missing — Epic 15's
epics.md entry lists exactly FR117/FR118 → 15-1, FR119 → 15-2, nothing else).

### Step 1.4: Reverse-Sequencing Guard

Scanned every epic `M < 15`: epics 1–14 are all `done` (epic 14 itself just re-confirmed done in
this same session's prior retro round). No earlier epic mid-flight — clean.

### Step 1.5: Prior Retro Action-Item Follow-Through

First retro (`epic-15-retro-2026-07-27.md`) items, re-verified against disk today:

| Finding | Artifact asked for | Found on disk today? |
|---|---|---|
| Medium — settings-PATCH handler duplication (3 instances) | Tracked as debt only, refactor trigger at a 4th instance | ✅ Still exactly 3 route registrations / 2 handler functions (`updateOrgDormancyColumn` x2, `updateOrgDefaultLocaleColumn` x1) in `organization-settings-routes.ts` — no 4th instance added since, trigger not yet hit, correctly still just tracked debt |
| Low — `GET /me`'s hardcoded locale enum | Documented follow-up, not a story | Not independently re-verified this round — cosmetic, low severity, unchanged scope |
| Low — `es.json` missing one translation key | Confirmed intended per-string-fallback, not a regression | No new evidence this changed |
| P5-1 (`z.union` ordering) — resolved same session as first retro | `no-error-schema-first-in-union.js` lint rule | ✅ Present, wired into `apiEnforcement`, actively exercised by this session's own 14-8 code review (same file family) |
| A12-3 (deferred-work DoD checklist) — resolved same session as first retro | Deferred-Work Tracking bullet in `bmad-dev-story` checklist + `check-story-references.ts` | ✅ Both confirmed present on disk (`scripts/check-story-references.ts` + `.test.ts` found directly this session) |

All prior items accounted for. Nothing regressed.

### Step 2: Gap & Risk Audit (this round's own research)

**1. Cross-file consistency.**
- `15-1`/`15-2` story `Status:` headers both `done`, matching `sprint-status.yaml`. Clean.
- **`epic-15`'s own rollup key is `in-progress` in `sprint-status.yaml`, despite both its stories
  and its retrospective being `done` since 2026-07-27** — this is the Critical finding. Concrete
  evidence: `sprint-status.yaml` line ~261 reads `epic-15: in-progress # registered 2026-07-23 ...
  Flipped to in-progress 2026-07-26 via bmad-create-story — 15-1 story file created`, with no
  subsequent entry ever flipping it to `done`, even though `15-2: done` (2026-07-27) and
  `epic-15-retrospective: done` (2026-07-27) both post-date that stale value by a full day. This is
  the same defect class Epic 14's second retro (`epic-14-retro-2026-07-27.md` Finding 1) explicitly
  named: "the rollup key itself was never flipped after [the last story] landed." **Resolution:
  fixed directly** — `epic-15: in-progress -> done` in this session's `sprint-status.yaml` commit,
  since G2's gate conditions (all stories done, retrospective done, no open Critical product-surface
  gaps) are all independently confirmed satisfied above.

**2. Requirement / UI coverage gaps.** Both FR117/118 (15-1: language selection + rendering) and
FR119 (15-2: org default locale) are fully web-consumable — `Settings > Language` (15-1) and
`Settings > Users` (15-2) both have real, live UI controls, confirmed by both stories' own
Product Surface Contract sections and live-browser verification notes in their Dev Agent Records.
No API-only gap.

**3. Process pattern carryover.** The rollup-key-drift Critical finding above is itself a
**recurring pattern** — the same defect class as Epic 14's Finding 1 (2026-07-27) and, further
back, Epic 1's original 2026-07-09 rollup-drift note and Epic 6's "flipped back" pattern. It does
not carry a prior `[REPEAT Nx]` tag under this skill's own tracking because no prior retro doc
raised *this specific epic's* rollup key as an open action item before today (each occurrence so
far has been caught and fixed within the same retro that found it, epic-by-epic, rather than
recurring across retros for the *same* epic) — so per this skill's Step 2 category-3 rule ("a
finding that recurs from a *prior retro's action items*"), this doesn't meet the `[REPEAT]` bar
for Epic 15 specifically. It is, however, now the **fourth epic** in this project's history to hit
this exact rollup-drift shape (epic-1, epic-6, epic-14, now epic-15) — worth flagging as a
candidate for a real enforced check (see Key Takeaways) rather than relying on every epic's retro
to independently rediscover and hand-fix it.

**4. Technical debt & duplication.** No new debt found. The one tracked item (settings-PATCH
handler duplication) is unchanged, trigger not yet hit (see Step 1.5 table above).

**5. Project-specific invariants.** Spot-checked `15-2`'s `PATCH
/api/v1/organizations/:orgId/default-locale-settings` and `15-1`'s `PATCH /api/v1/users/me/locale`
against this project's fail-closed-audit and tenant-isolation invariants — both use `secureRoute()`
with `minimumRole`/self-only IDOR guards and fail-closed audit writes, consistent with the rest of
the codebase. No violation found.

### Steps 3–10: Epic Review, Next-Epic Prep, Action Items, Closure

As with Epic 14's fourth round (same session), this closure round does not re-run the full roleplay
ceremony a second time for the same reason: the substantive successes/challenges content from the
first retro (2026-07-27, run live with Nestor through the Epic Review Discussion) remains accurate
and unchanged — nothing about Epic 15's actual delivery history changed between then and now, only
that its rollup key's own bookkeeping was found stale.

**Epic 16 dependency check (Epic 15's "next epic" by number):** Epic 16 (Custom Theming) is already
`in-progress` (started 2026-07-27, independent of Epic 15 per `epics.md`'s own split rationale — no
shared backend module or data model between i18n and theming). No blocking dependency found.

## Readiness Assessment

Epic 15 is genuinely complete: both stories `done` with clean adversarial passes, retrospective
`done`, G2 gate conditions all satisfied, and — as of this retro — the rollup key itself now
correctly reflects that. No outstanding blockers.

## Action Items

**Fixed directly this session (not scheduled as a story):**
1. `epic-15: in-progress -> done` in `sprint-status.yaml` — Critical finding above.

**Flagged for Nestor, not auto-scheduled** (same reasoning as Epic 14's round-4 retro, run
immediately before this one in the same session): the rollup-key-drift pattern has now recurred
independently across 4 epics (1, 6, 14, 15). A real enforced check — e.g. a `make ci` guard that
fails if `sprint-status.yaml` claims `epic-N: in-progress` (or any non-`done` value) while every
`N-*` story key plus `epic-N-retrospective` are all `done` — would close this permanently instead
of relying on each epic's own retro to independently rediscover it. This is a genuinely new idea
(not previously proposed in any prior retro doc searched this session), so per this skill's own
"don't manufacture findings" and "HALT before writing to sprint-status.yaml without confirmation"
principles, it is surfaced here as a question for Nestor rather than scheduled unilaterally.

## Key Takeaways

1. Two clean, small, well-scoped stories (15-1, 15-2) with strong elicitation and adversarial
   review produced the cleanest per-story-content audit in this project's history — the only defect
   found across two full retro rounds was bookkeeping (the rollup key), not the shipped code or
   requirements coverage itself.
2. Rollup-key drift is now a confirmed 4-epic recurring pattern (epic-1, epic-6, epic-14, epic-15).
   Each instance was caught and fixed within the epic's own retro — never shipped or discovered by
   a user — but the fact that it keeps recurring independently suggests a mechanical check would be
   more reliable than relying on retro audits to catch it every time.

## Next Steps

1. Continue to the chained task's next step: create any gap-closing stories (none needed for either
   epic 14 or 15 this round — both closure audits came back clean of new schedulable work), then
   rebase/push/PR.
2. Consider Nestor's input on the rollup-key-drift CI-guard idea (Key Takeaway 2, and the identical
   idea raised independently in Epic 14's round-4 retro doc for the `review->done` drift class) —
   both are the same underlying "tracking metadata can silently drift from reality" risk, just
   different granularities (story-status vs. epic-rollup). Worth considering as one combined guard
   rather than two.

---

*Sprint status: `epic-15-retrospective` remains `done` (no status change — already recorded `done`
since the first retro). `epic-15` itself flips `in-progress -> done` this session — see
`sprint-status.yaml`'s top-of-file log for this round's entry.*
