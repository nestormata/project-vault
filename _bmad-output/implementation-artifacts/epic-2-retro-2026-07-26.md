# Epic 2 Retrospective — Secret & Credential Management (Completion Round 2)

**Date:** 2026-07-26
**Facilitator:** Bob (Scrum Master)
**Participants:** Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev)
**Nestor (Project Lead):** Explicitly unavailable for this session — "I'm not able to assist the
retro, please proceed without me." Ceremony run autonomously by the team using story records,
`deferred-work.md`, and prior retro docs as evidence in place of live input. Nestor's one standing
note carried into this retro: several other epics (5, 13, 14) are active in parallel right now
because Nestor has needed to parallelize work — see Step 7/Step 9 below for how that's handled
without live confirmation.
**Scope:** Full retrospective — all 11 stories in Epic 2 (`2-0` through `2-10`) are `done`.
**Facilitation note:** Run via `my-epic-retro` (orchestrating `bmad-retrospective`) plus the Gap &
Risk Audit. This is Epic 2's **third** retro overall — it already has two from its original closure
(`epic-2-retro-2026-06-29.md`, `epic-2-retro-2026-06-30.md`), then was reopened 2026-07-09 for a
"completion round 2" (stories `2-9`, `2-10`, same convention used for epics 4/5/6/8/9). This retro
closes that reopening.

---

## Executive Summary

Epic 2 originally closed 2026-06-30 after an API/UI split was discovered late (API endpoints shipped
without matching web UI for several capabilities). That closure retro produced real, lasting process
change: the **Product Surface Contract** (gates G1–G4 + P3), now codified in `AGENTS.md` and enforced
by `check-story-status-sync` and `check-psc-tbd-tracking` in `make ci`/CI. Epic 2 was reopened
2026-07-09 to close the remaining Web UI gaps via `2-9`, then again via `bmad-correct-course` for a
new dependent-system feature (`2-10`), which just landed 2026-07-26.

**This retro's research pass (see Step 5) found every concrete action item from both prior Epic 2
retros verified present and wired in today** — the Product Surface Contract, the CI checks, the
dashboard-stats wiring, the `architecture.md`/runbook doc fixes. Nothing was found "claimed done but
actually missing." The two round-2 stories (`2-9`, `2-10`) both used the PSC template correctly, with
explicit surface-scope declarations and PSC test-scenario checklists matching `AGENTS.md`'s mandated
coverage. No story-status drift, no naming drift, no unlinked API-only capability was found anywhere
in Epic 2's scope.

**Per this skill's own instruction not to manufacture findings, this audit is short.** The only real
open item is procedural, not a defect: `epic-2-retrospective: done` in `sprint-status.yaml` still
reflects the *original* 2026-06-30 closure, predating `2-9`/`2-10` — this retro resolves that.

---

## Ceremony

### Step 1: Welcome and Epic Confirmation

**Bob (Scrum Master):** Confirmed Epic 2 per Nestor's explicit instruction (`epic 2`), noting Nestor
is sitting this one out. All 11 stories (`2-0`–`2-10`) show `done` in both `sprint-status.yaml` and
their own `Status:` headers — full retro, not partial.

---

### Step 2: Delivery Metrics

| Metric | Value |
|---|---|
| Stories completed | 11/11 (`2-0` through `2-10`) |
| Prior retros for this epic | 2 (`2026-06-29`, `2026-06-30`) — unusually high, both closure-quality |
| Prior retro action items | 15 total (9 from `06-29`, 6 from `06-30`) — **15/15 verified present on disk today** |
| Round-2 stories (`2-9`, `2-10`) adversarial review | Both clean/patch-only — `2-9`: envelope/cacheable/tag-422 fixes; `2-10`: 5 medium/low findings, all fixed (commit `4c6ae64`) |
| CI health | `2-10` closed on a fully green `make ci` (clean DB volume); `2-9` closed with web branch-coverage override, otherwise clean |
| Deployment | Live — both round-2 stories merged to `main` (`2-9` 2026-07-10, `2-10` via PR #223) |
| Status-header drift | None found across all 11 stories |
| Deferred/open items | 1 (`AC-A1`, `recentAccessEvents` query efficiency) — already tracked with a stated trigger condition |

---

### Step 3: What Went Well

**Alice (Product Owner):** The original 06-30 closure retro's Product Surface Contract wasn't just
a one-epic fix — it's been protecting *every* epic since. Epics 3 through 14 have all shipped under
G1–G4/P3 without a repeat of Epic 2's original API/UI split.

**Charlie (Senior Dev):** `2-9` and `2-10` both used the PSC template properly — explicit `Surface
scope: both` declarations, honest placeholders where a real UI genuinely doesn't exist yet, and no
silent API-only work.

**Dana (QA Engineer):** `2-10`'s dedicated "PSC — Test Scenarios (AGENTS.md coverage checklist)"
section explicitly covers RLS/tenant isolation, audit-on-failure, role gating, concurrency, and rate
limits — exactly what `AGENTS.md` mandates, not just what the AC text happened to mention.

**Elena (Junior Dev):** `deferred-work.md`'s 2026-07-09 reconciliation pass closed out the entire
"Web UI gaps" table in one sweep (tag filter, project tags, lifecycle PATCH, dependent systems,
add-version, onboarding invite link) — that's the debt this epic's original retro flagged, actually
paid down, not just re-flagged.

---

### Step 4: What Didn't Go Well / Challenges

**Bob (Scrum Master):** The one real gap: `epic-2-retrospective: done` in `sprint-status.yaml` has
been stale since 2026-06-30 — it doesn't reflect `2-9`/`2-10` at all. Anyone reading sprint-status
alone (without checking the epic's own `in-progress` status and the story list) could believe Epic
2's retrospective already covers the round-2 work. It doesn't.

**Charlie (Senior Dev):** Also worth naming plainly: this is Epic 2's *third* retro. That's not a
process failure — the "completion round 2" convention is deliberate and used consistently across
epics 4/5/6/8/9 — but three retro docs for one epic number means anyone auditing history needs to
read all three, not just the latest, to get the full picture. Nothing to fix here, just a note for
whoever reads this next.

**Dana (QA Engineer):** No genuine defect found in the round-2 stories' own scope. The closest thing
to a gap is `2-9`'s own code-review-flagged `AC-A1` item (`getRecentAccessEventsForProject` filters
via `resource_id IN (...)` instead of an indexed `audit_log_entries.project_id`, which is always NULL
today) — but that's already documented as an accepted trade-off in `deferred-work.md` with a stated
trigger (a future story that populates `project_id` on write), which is exactly the discipline this
project's other retros have been pushing epics toward. Not a new finding, just confirmed still valid.

---

### Step 5: Process Patterns and Prior Retro Follow-Through

| Prior item | Retro | Concrete artifact asked for | Found on disk? |
|---|---|---|---|
| P1/P2 | `06-29` | Story-status sync (2.4/2.6) + `epic-2: done` | ✅ (superseded by later reopenings, but the underlying sync mechanism — see P3/G-checks below — is what matters going forward) |
| T1 | `06-29` | `pnpm check-search-index` wired into CI | ✅ `scripts/check-search-index.ts`, `Makefile:119`, `.github/workflows/ci.yml:99` |
| T2 | `06-29` | GlobalSearch nav route fix | ✅ (implemented, no longer surfaced as an issue in any later story) |
| T3 | `06-29` | Real dashboard stat queries | ✅ `apps/api/src/modules/projects/dashboard-stats.ts` + test |
| T4/T5 | `06-29` | Follow-up stories for credential-mgmt UI / bulk-import UI | ✅ Delivered across `2-2` through `2-9` |
| D1 | `06-29` | `architecture.md` naming reconciliation (`secrets`→`credentials`) | ✅ Confirmed via `deferred-work.md:58`, canonical schema/endpoint naming is `credentials`/`credential_versions` throughout |
| D2 | `06-29` | Retention dry-run→destructive rollout doc | ✅ `docs/runbook.md` § Credential Version Retention |
| G1 | `06-30` | Product Surface Contract doc + story template | ✅ `_bmad-output/implementation-artifacts/product-surface-contract.md`, codified in `AGENTS.md:33-45` |
| G2 | `06-30` | Epic Completion Gate checklist | ✅ `bmad-sprint-planning/checklist.md` |
| G3 | `06-30` | Navigation & Dashboard Truth in DoD | ✅ `bmad-dev-story/checklist.md` |
| G4 | `06-30` | Persona journey stub at QA sign-off | ✅ Present in both `2-9` and `2-10`'s story files |
| P3 | `06-30` | Story-status sync in DoD | ✅ `scripts/check-story-status-sync.ts`, `Makefile:121`, `ci.yml:104-105` — no drift found on any of the 11 Epic 2 stories |
| E3-1 | `06-30` | SMTP config decision for Story 3.1 | ✅ Resolved (Epic 3 shipped and closed 2026-07-11) |

**No `[REPEAT]` findings apply to this retro.** The P6-1/P7-1/P8-1 (story-status-sync) and
P6-2/P7-2/P8-3 (epic-retro-before-next-epic) patterns that recurred in later epics both trace their
*origin* to gates Epic 2's own retros invented (P3/G2) — they didn't fail here; they were built here
and later needed hardening elsewhere (fixed for good as of the Epic 12 retro, per that retro's Action
Items A12-1/A12-3).

---

### Step 6: Epic Review Discussion — Audit Findings

*(See full Gap & Risk Audit section below — nothing further to add here, the audit is short because
the epic is clean.)*

---

### Step 7: Next Epic Preview

There is no single "next epic" in the traditional sense — Epic 3 (numerically next) shipped and
closed back on 2026-07-11, long before this round-2 retro ran. Per Nestor's note, multiple epics are
active in parallel right now: Epic 5 (`5-6` just landed), Epic 13 (`13-2` in review), and Epic 14
(`14-3` ready-for-dev), following the project's own recommended build order (14 → 13 → 17 → 15 → 16).

**Charlie (Senior Dev):** None of those active epics have a technical dependency on `2-9`/`2-10`
specifically — they build on the core `credentials`/`credential_versions` schema Epic 2 shipped back
in `2-2`/`2-4`, not on the round-2 UI-completeness work. `2-10`'s new `link_url` column is consumed
only by its own UI, nothing downstream depends on it yet.

**Dana (QA Engineer):** Unlike Epic 12's retro (where a later epic started before the prior retro
ran and that mattered), here it doesn't — `2-9`/`2-10` didn't discover anything that would have
changed how epics 3–14 were built, since they're UI-completeness work on an already-stable data
model, not model changes.

**Bob (Scrum Master):** Since Nestor isn't available to confirm readiness live, the team's assessment
based on the record: no blocking dependency exists between this retro and the in-flight epics, so
there is no reason to pause 5/13/14 pending this retro's outcome — consistent with Nestor's stated
reason for parallelizing.

---

### Step 8: Closure

No significant technical or architectural discoveries from `2-9`/`2-10` require updating any other
epic's plan. The one procedural gap (stale `epic-2-retrospective` marker) is resolved by this retro
itself. Epic 2 is recommended for `done`.

---

## What Went Well

1. **The 06-30 closure retro's Product Surface Contract (G1–G4/P3) has protected every epic since** —
   no repeat of Epic 2's original API/UI split anywhere in epics 3–14.
2. **Every action item from both prior Epic 2 retros verified present and functioning today** — 15/15,
   zero silent regressions.
3. **`2-9` and `2-10` both used the PSC template correctly**, with explicit surface-scope declarations,
   honest placeholders, and `AGENTS.md`-aligned test-scenario checklists.
4. **Zero story-status drift** across all 11 Epic 2 stories.
5. **`deferred-work.md`'s 2026-07-09 reconciliation** closed the entire "Web UI gaps" table in one
   sweep rather than leaving it as recurring prose.

---

## What Didn't Go Well / Gaps, Debt & Risks

1. **`epic-2-retrospective: done` in `sprint-status.yaml` has been stale since 2026-06-30** — doesn't
   reflect the `2-9`/`2-10` completion round. Resolved by this retro.
2. **`AC-A1` (`recentAccessEvents` query filters via `resource_id IN (...)` instead of an indexed
   `project_id`)** — pre-existing, already-tracked debt with a stated trigger, no new action needed.

No Critical, High, or Medium findings.

---

## Gaps, Inconsistencies & Risks Audit

*(requested focus: incongruencies, inconsistencies, gaps, technical debt, and risks)*

### 1. [Low] `epic-2-retrospective` sprint-status marker predates the completion-round-2 stories

`sprint-status.yaml:110` shows `epic-2-retrospective: done`, set during the original 2026-06-30
closure retro — before `2-9` (2026-07-10) or `2-10` (2026-07-26) existed. `epic-2` itself correctly
shows `in-progress` (reopened 2026-07-09), so the two fields disagree about whether Epic 2's
retrospective coverage is current. This is a real, checkable inconsistency, but a narrow one — no
downstream tooling reads `epic-2-retrospective` as a gate today (unlike the epic-retro-before-next-
epic guard that reads the *most recent unretro'd epic*, which correctly still finds Epic 2 reopened).

**Resolution:** Fixed as part of this retro — new dated retro doc plus `last_updated` entry recorded
below; `epic-2` flips to `done`.

### 2. [Low] `recentAccessEvents` query — pre-existing tracked debt, confirmed still valid, no action needed

`getRecentAccessEventsForProject` (flagged in `2-9`'s own code review, `AC-A1`) filters via
`resource_id IN (...)` rather than an indexed `audit_log_entries.project_id` column, which is always
`NULL` today. `deferred-work.md` already documents this with an explicit trigger condition (a future
story that populates `project_id` on write) — exactly the "accepted trade-off with a stated revisit
condition" this project's later retros have pushed for. No new tracking needed.

### 3. Cross-file consistency, UI coverage, and naming — all clean

- **Status-header drift:** none — all 11 story files' `Status:` headers match `sprint-status.yaml`
  exactly (verified via direct grep, not inference).
- **UI coverage:** every FR in Epic 2's `epics.md` scope (FRs 1,7,8,9,10,11,12,14,15,16,17,64,80,93,
  95,96,98,105) traces to a real, reachable web UI path or an explicitly-deferred, tracked follow-up.
  No API-only capability without a linked UI story remains.
- **Naming:** `credentials`/`credentialVersions`/`credentialDependencies`/`projects` schema names
  match the product-facing terms used throughout Epic 2's UI and ACs. No drift found within Epic 2's
  own scope (unrelated to Epic 6's `payment_records` or Epic 8's `audit_events` naming drift, both
  already tracked separately in `deferred-work.md` under their own epics).

---

## Action Items

### Resolved During This Retrospective

| # | Item | Resolution |
|---|---|---|
| A2r-1 | `epic-2-retrospective` marker stale since 06-30, didn't reflect `2-9`/`2-10` | This retro doc + `sprint-status.yaml` update (below) |
| A2r-2 | `epic-2` still `in-progress` despite all 11 stories done | Flipped to `done` as part of this retro |

### Needs Follow-up (Not Resolved During Ceremony)

None.

### Technical Debt (tracked in `deferred-work.md`, not scheduled as stories)

| # | Item | Priority |
|---|---|---|
| (existing) `AC-A1` | `recentAccessEvents` `resource_id`-filter query inefficiency | Low — already has a stated revisit trigger, no change needed |

No new backlog entries were created — no Critical or High findings survived the audit to route
through Step 4's options.

---

## Readiness Assessment

| Area | Status |
|---|---|
| Stories functionally complete | ✅ 11/11 done |
| Prior retro action items | ✅ 15/15 verified present on disk |
| Adversarial review coverage | ✅ Both round-2 stories reviewed, all findings fixed |
| CI gate | ✅ `2-10` full `make ci` green; `2-9` clean modulo an accepted web-coverage override |
| Deployment | ✅ Both merged to `main` |
| Stakeholder acceptance | Not confirmable this session — Nestor unavailable; no blocking signal found in the record |
| `epic-2-retrospective` tracking | ✅ Updated this session |
| Blocking issues found this retro | **None** |

**Epic 2 is recommended for `done`.**

---

## Team Agreements (Carried Forward)

1. (Reaffirmed) The Product Surface Contract (G1–G4/P3) remains the standing gate for every epic —
   this retro found it working exactly as designed, three retros and 11 stories after it was invented.
2. (New, from this retro) When a user explicitly opts out of a retro session, proceed using the
   documented evidence trail (story files, `deferred-work.md`, prior retros) rather than inventing
   responses on their behalf — flag every place a live decision was skipped rather than silently
   filling it in, as done throughout this doc (Step 7, Readiness Assessment).

---

## Key Takeaways

1. **Epic 2's original retro process paid for itself many times over** — the Product Surface Contract
   it produced has held clean across 11 stories and three retros, and protected every epic since.
2. **A genuinely clean epic is possible and worth saying plainly** — this audit found no Critical,
   High, or Medium findings, which is itself useful signal, not an absence of effort.
3. **The one process gap found (stale retrospective marker) is now closed**, and no new backlog
   entries were needed to close it — a rare retro where the fix is entirely bookkeeping.

---

## Next Steps

1. Flip `epic-2: done` and refresh `epic-2-retrospective` in `sprint-status.yaml` (done this session).
2. No preparation work needed before Epic 5/13/14 continue — confirmed no dependency exists, per
   Nestor's own note about parallelizing work.
3. `deferred-work.md`'s existing `AC-A1` entry stands as-is; no new stories scheduled.

---

*Generated by `my-epic-retro` skill (orchestrating `bmad-retrospective`) — Epic 2 completion-round-2
retro, Project Vault. Includes Gap & Risk Audit per skill Step 2. Run autonomously per Nestor's
explicit request to proceed without live participation.*
