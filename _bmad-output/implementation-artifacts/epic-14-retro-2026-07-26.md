# Epic 14 Retrospective — Extension Architecture & Pluggable Authentication

**Date:** 2026-07-26
**Facilitator:** Bob (Scrum Master)
**Participants:** Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev)
**Nestor (Project Lead):** Explicitly unavailable — "proceed with the repo without me." Ceremony run
autonomously using story records, `deferred-work.md`, and direct codebase verification in place of
live input, same convention as the Epic 1/2/5 retros run earlier this session.
**Scope:** Full retrospective — all 5 stories in Epic 14 (`14-0` through `14-4`) are `done`. **First
retrospective for this epic.**

---

## Executive Summary

Epic 14 shipped the AGPLv3/CLA relicense, the extension-api package, the fail-safe extension loader, a
registered-external-provider auth strategy, and email-domain SSO routing — all five stories closed with
clean or fixed-in-review adversarial passes and green `make ci`.

**This retro found one real, unresolved Critical gap that this session cannot close on its own
authority: the extension status admin UI page.** Story `14-2`'s own Product Surface Contract flagged —
in writing, at story-creation time — that `GET /api/v1/admin/extensions/status` (and `GET /health`'s
`extensions_status` field) ship with **no UI to view them**, and explicitly named this a **G2 blocking
note: epic-14 may not reach `done` until a UI page or equivalent is scheduled.** `deferred-work.md`
(lines 98) still lists this as `Open — must be scheduled`. A direct search confirms it: no
`(app)/admin/extensions/` route exists anywhere in `apps/web/src/routes`, and no story numbered
`14-5` or later exists in `sprint-status.yaml` or `epics.md`.

**Because of this, this retro does not flip `epic-14` to `done`.** A new backlog story is scheduled
(`14-5`) instead — this is exactly the class of finding this skill's Step 4 exists to convert into a
tracked story rather than leaving it as retro prose, and the epic's own G2 gate is the authority saying
it must be closed before completion, not a judgment call this session is overriding.

The SSO login UI gap (`deferred-work.md` line 99, tracked against `14-4`) is now resolved — `14-4`
shipped and closed it.

---

## Ceremony

### Step 1: Welcome and Epic Confirmation

**Bob (Scrum Master):** Confirmed Epic 14 per Nestor's explicit instruction. All 5 stories (`14-0`
through `14-4`) show `done` in both `sprint-status.yaml` and their own `Status:` headers — story-level
completeness is real, but see the epic-level gap below before treating the epic itself as closable.

---

### Step 2: Delivery Metrics

| Metric | Value |
|---|---|
| Stories completed | 5/5 (`14-0` through `14-4`) |
| Prior retros for this epic | 0 — first retro |
| Adversarial review coverage | 5/5 stories reviewed; `14-0` (1 high fixed — CLA action targeted protected `main`), `14-1` (1 high fixed — version-skew guard read branch-tip instead of merge-base), `14-2` (clean, 0 critical/high), `14-3` (3 high fixed — cross-org identity ambiguity, deactivated-member SSO bypass, timing side-channel), `14-4` (1 high fixed — stale pre-existing login test broke silently) |
| CI health | All 5 landed green; `14-3`/`14-4` each needed 2-4 fix rounds before green, all resolved same-session |
| Deployment | Live — all 5 merged to `main` |
| Status-header drift | None — repo-wide `pnpm check-story-status-sync` clean |
| Open G2 gate | **1 — extension status admin UI page, flagged since `14-2` (2026-07-24), still unscheduled** |
| CLA follow-up (sandbox-inherent) | 2 items need repo-admin GitHub access (branch-protection wiring, live test-PR verification) — tracked for Nestor, not this session's to close |

---

### Step 3: What Went Well

**Alice (Product Owner):** Every story in this epic correctly used the Product Surface Contract to
flag its own gaps rather than silently shipping API-only work — `14-2` and `14-3` both named their UI
gaps explicitly in writing at story-creation time, which is exactly why this retro could find the
extension-status gap by reading the record rather than having to rediscover it from scratch.

**Charlie (Senior Dev):** `14-3`'s adversarial review is a strong example of security-critical review
depth — cross-org identity ambiguity, a deactivated-member SSO bypass, and a timing side-channel are all
genuine auth vulnerabilities, all caught and fixed before merge, not left as accepted debt.

**Dana (QA Engineer):** `14-4`'s review caught a real, silent regression: the pre-existing
`(auth)/login/page.test.ts` broke 4 of its own 9 tests the moment the new two-step `LoginForm` shipped,
and the story's initial test run never exercised the full `apps/web` suite to notice. Fixed before
merge, full suite confirmed 194/194 files green afterward — a good reminder that "my new tests pass"
isn't the same bar as "the suite I touched still passes."

**Elena (Junior Dev):** `14-0`'s adversarial review caught a genuinely epic-breaking process bug (the
CLA-signature GitHub Action was configured to commit directly to protected `main`, which would have
silently broken CLA enforcement forever) before it ever ran once in production.

---

### Step 4: What Didn't Go Well / Challenges

**Bob (Scrum Master):** The extension status admin UI page — see Executive Summary. This is the epic's
one real, unresolved gap, and it's been sitting flagged-but-unscheduled since `14-2` landed on
2026-07-24, two days before this retro.

**Charlie (Senior Dev):** `14-0` also left two items genuinely incomplete for a structural reason (not
negligence): configuring the CLA check as a required branch-protection status check on `main`, and a
live test-PR verification from a fresh unsigned account — both need repo-admin GitHub access this
sandboxed environment doesn't have. Correctly documented, not silently dropped, but still open and
needs Nestor to close manually.

**Dana (QA Engineer):** No new defect found beyond what's already listed above.

**Elena (Junior Dev):** Worth naming: this is the first epic in the project where a G2 gate was flagged
at story-creation time (`14-2`) and then *not* closed by the time all of that epic's numbered stories
reached `done` — every other epic that hit a G2 gap (Epic 2's original API/UI split, Epic 7/8/9's admin
UI gaps) closed it with a dedicated follow-up story before or during the retro that found it. This retro
is what closes the pattern here too, by scheduling `14-5` now rather than letting it slip further.

---

### Step 5: Process Patterns and Prior Retro Follow-Through

No prior Epic 14 retro exists — this category is skipped per this skill's own error-handling table
("No previous epic retro exists → skip the process-pattern-carryover audit category silently").

**However, the G2-gate pattern itself recurs across this project's history** (Epic 2's original
API/UI split; Epic 7's Finding — G2 gap closed by `8-6`; Epic 8's Finding 2 — G2 gap closed by `8-7`;
Epic 9's Finding 1, explicitly tagged `[REPEAT 4x]` in that epic's own retro, closed by `9-7`). Epic
14's extension-status gap is the same *class* of finding recurring in a new epic, not a repeat of a
specific unresolved item from a named prior retro — so it is not tagged `[REPEAT]` under this skill's
strict definition (that tag requires recurrence of a specific action item across *this epic's own*
prior retros, of which there are none yet). It is, however, exactly the failure mode those `[REPEAT]`
tags exist to prevent, and is treated with the same seriousness: scheduled now, not deferred again.

---

### Step 6: Epic Review Discussion — Audit Findings

*(See full Gap & Risk Audit section below.)*

---

### Step 7: Next Epic Preview

**Bob (Scrum Master):** Epic 13 is next per the recommended build order (14 → 13 → 17 → 15 → 16) and is
already in-progress in parallel. Epic 17 (Secret Sharing & Exposure Tracking) is registered backlog and
explicitly flagged to require a dedicated security-review pass before story creation begins, given it
introduces the product's first unauthenticated external-share pathway — not this retro's scope to
start, just worth carrying forward.

**Charlie (Senior Dev):** No in-flight epic has a technical dependency on the extension-status UI page
specifically — `14-5` can be scheduled and built independently of Epic 13's own work.

---

### Step 8: Closure

One real, unresolved gap (the extension status admin UI page) blocks recommending `epic-14` for `done`
per its own `14-2`-flagged G2 gate. This retro schedules `14-5` to close it and leaves `epic-14`
`in-progress` until `14-5` lands — consistent with how every other G2 gap in this project's history has
been handled (a dedicated follow-up story, not a silent closure).

---

## What Went Well

1. **Every story in this epic used the Product Surface Contract correctly** — `14-2`/`14-3` both flagged
   their own UI gaps in writing at creation time rather than shipping silently API-only.
2. **Adversarial review caught real, security-relevant bugs in 4 of 5 stories** — a broken CLA-enforcement
   Action, a version-skew guard bug, three auth vulnerabilities in `14-3`, and a silent test regression
   in `14-4` — all fixed before merge.
3. **Zero story-status drift** across all 5 Epic 14 stories, and repo-wide.

## What Didn't Go Well / Gaps, Debt & Risks

1. **Extension status admin UI page — flagged since `14-2` (2026-07-24), still unscheduled.** This is a
   real G2 blocking gap, not a nitpick — Critical. Resolved by scheduling `14-5` in this retro.
2. **Two `14-0` CLA-hardening items need repo-admin GitHub access** this environment doesn't have —
   tracked for Nestor, not silently dropped, but still genuinely open.

---

## Gaps, Inconsistencies & Risks Audit

*(requested focus: incongruencies, inconsistencies, gaps, technical debt, and risks)*

### 1. [Critical] Extension status admin UI page — flagged G2 gate, unscheduled for 2 days, epic cannot honestly close without it

`14-2`'s own Product Surface Contract (recorded in `sprint-status.yaml`'s 2026-07-24 `last_updated`
entry and `deferred-work.md` line 98) states: *"no story in the current sprint backlog (14.0-14.4, or
any other tracked epic) builds the page ... flagged this explicitly as a blocking note for `epic-14`
before it may reach `done`."* Verified directly: no `(app)/admin/extensions/` route exists anywhere
under `apps/web/src/routes`, and no story numbered `14-5` or later exists in `sprint-status.yaml` or
`epics.md`. `GET /api/v1/admin/extensions/status` and `GET /health`'s `extensions_status` field are
real, callable, and honest about empty state — but an OrgAdmin today can only check extension status
via `curl`/`httpie`, exactly as `14-2`'s own flag warned.

**Resolution:** Scheduled as a new backlog story, `14-5-extension-status-admin-page` (see Action Items
below). `epic-14` stays `in-progress` until it lands — this retro does not close the epic.

### 2. [Low] Two CLA-hardening items remain incomplete for a structural (sandbox) reason, not negligence

`14-0`'s branch-protection wiring and live test-PR verification both need repo-admin GitHub access this
environment doesn't have. Already correctly documented as open in `sprint-status.yaml`'s `14-0` entry,
not silently dropped.

**Resolution:** No action taken this retro — genuinely requires Nestor's manual follow-through outside
any agent session. Left as tracked, not routed to a story (nothing to implement, it's a GitHub UI
configuration task).

### 3. Cross-file consistency, UI coverage, and naming — otherwise clean

- **Status-header drift:** none — repo-wide `pnpm check-story-status-sync` passes clean for all 5 Epic
  14 stories.
- **UI coverage (beyond Finding 1):** the SSO login-routing UI gap flagged against `14-3`
  (`deferred-work.md` line 99) is now closed — `14-4` shipped it, confirmed via live Chrome verification
  per its own story record.
- **Naming:** `org_sso_domains`, `external_identities`, extension manifest naming all match the
  product-facing language used in this epic's ACs and docs. No drift found.

### 4. Technical debt & duplication — none new found

No repeated structural patterns or un-consolidated workarounds found across this epic's 5 stories
beyond what each story's own adversarial review already caught and fixed in-story.

### 5. Project-specific invariants — spot-checked, no violations found

- Tenant isolation: `org_sso_domains`/`external_identities` both carry `org_id` with RLS, consistent
  with the project's existing pattern.
- Fail-closed audit logging: `14-2`'s per-org audit fanout isolates failures per-org (won't crash boot
  or block other orgs); `14-3`'s SSO success/reject audit events are mandatory per its own AC.
  Consistent with `AGENTS.md`'s fail-closed invariant.
- Auth/session checks: `14-2`'s admin-status route requires `requireMfa: true` + `allowedRoles: ['admin']`
  only (not `owner`), a deliberate, documented judgment call, not an oversight.

---

## Action Items

### Resolved During This Retrospective

| # | Item | Resolution |
|---|---|---|
| A14r-1 | `epic-14-retrospective` had no prior entry recorded | This retro doc + `sprint-status.yaml` update |

### Needs Follow-up (Not Resolved During Ceremony)

| # | Item | Owner | Note |
|---|---|---|---|
| A14r-2 | CLA branch-protection wiring + live test-PR verification | Nestor | Needs repo-admin GitHub access unavailable in this environment |

### New Backlog Story

| ID | Item | Priority |
|---|---|---|
| `14-5-extension-status-admin-page` | Build `(app)/admin/extensions/` page surfacing `GET /api/v1/admin/extensions/status`'s loaded/not-loaded/manifest state, closing the G2 gap `14-2` flagged at creation time | **Critical — blocks `epic-14` closure per its own G2 gate** |

Added to `sprint-status.yaml` under `epic-14` as `backlog`, per this skill's Step 4 (routed to option 1
— a closure story on the current epic, since the gap is Epic 14's own scope).

---

## Readiness Assessment

| Area | Status |
|---|---|
| Stories functionally complete | ✅ 5/5 done (`14-0`-`14-4`) |
| Adversarial review coverage | ✅ 5/5, all findings fixed |
| CI gate | ✅ All 5 landed green |
| Deployment | ✅ All merged to `main` |
| Product Surface Contract / G2 gate | ❌ **Open** — extension status admin UI page, now scheduled as `14-5` |
| Stakeholder acceptance | Not confirmable this session — Nestor unavailable; no blocking signal beyond the G2 gate itself |
| `epic-14-retrospective` tracking | ✅ Recorded this session |
| Blocking issues found this retro | **1 — Critical, the G2 gate above** |

**Epic 14 is NOT recommended for `done` this retro** — it stays `in-progress` pending `14-5`. This is a
deliberate deviation from the Epic 1/5 retros run in this same session (both of which did close), driven
by a real, epic-owned gate rather than a judgment call this retro is making unilaterally.

---

## Team Agreements (Carried Forward)

1. (Reaffirmed) Adversarial review remains mandatory for every auth/crypto/RLS/deployment story — held
   across all 5 Epic 14 stories, catching 4 stories' worth of real security/process bugs.
2. (Reaffirmed, from Epics 1/2/5 round-2 retros) When a user explicitly opts out of a retro session,
   proceed using the documented evidence trail rather than inventing responses on their behalf.
3. (New) A G2 gate flagged at story-creation time and not yet closed by the time all of an epic's
   numbered stories reach `done` should be closed at the next retrospective touching that epic, not
   left for a later session to rediscover — this retro is the direct application of that rule to its
   own findings.

---

## Key Takeaways

1. **This epic's stories individually shipped clean, adversarially-reviewed work** — the one real gap
   is at the epic level (a flagged-but-unscheduled UI page), not in any single story's own execution.
2. **The Product Surface Contract worked exactly as designed** — `14-2` flagged its own gap in writing,
   which is precisely why this retro could find and close it rather than having to reverse-engineer it.
3. **Not every epic in this session gets flipped to `done`** — this retro's job is to find the truth,
   not to complete a checklist; a real G2 gate is a legitimate reason to leave an epic open.

---

## Next Steps

1. `14-5-extension-status-admin-page` added to `sprint-status.yaml` as `backlog` under `epic-14`
   (done this session) — run `plan-story 14-5` next to flesh it out.
2. `epic-14` stays `in-progress` until `14-5` lands; `epic-14-retrospective` recorded as `done` for this
   ceremony, but a follow-up retro (or this same doc's addendum) should confirm closure once `14-5`
   ships, same pattern as Epic 5's addendum convention.
3. Nestor to manually complete `14-0`'s two repo-admin-only CLA hardening items when convenient.

---

*Generated by `my-epic-retro` skill (orchestrating `bmad-retrospective`) — Epic 14 first retrospective,
Project Vault. Includes Gap & Risk Audit per skill Step 2. Run autonomously per Nestor's explicit
request to proceed without live participation.*
