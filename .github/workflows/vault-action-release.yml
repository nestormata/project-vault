name: vault-action Release

# D7/AC-15 — the first tag-triggered release workflow in this repo (ci.yml/nightly.yml are both
# push/PR/schedule-triggered only). Builds/tests packages/vault-action, re-verifies dist/
# freshness AT THE TAGGED REF (not an ephemeral, never-compared rebuild — see AC-15's rationale),
# and only on success force-moves the mutable major-version convenience tag (e.g. pushing
# vault-action-v1.2.0 moves vault-action-v1 to the same commit), mirroring actions/checkout's own
# well-known "mutable major tag" convention.
#
# OPERATIONAL REQUIREMENT (cannot be enforced by this file's contents alone — D7): this workflow
# file and the branch it releases from must sit behind this repo's standard branch-protection /
# required-review settings, so moving the mutable vault-action-v1 tag always traces back to a
# reviewed change, not a unilateral push. Verify this repo-configuration step manually; flagged
# again in packages/vault-action/README.md and this story's Completion Notes.
#
# MARKETPLACE MIRROR (issue #112): GitHub Marketplace requires action.yml at a repository root,
# which a monorepo subdirectory can never satisfy. The final step below pushes the built
# action.yml + dist/ to the standalone nestormata/vault-action repo (build-mirror only, no
# independent source) using the VAULT_ACTION_MIRROR_TOKEN repo secret — a fine-grained PAT scoped
# to Contents: write on that repo only. If that secret is missing/expired the mirror step fails
# but the tag/major-tag move above it has already succeeded.

on:
  push:
    tags:
      - 'vault-action-v*'

permissions:
  contents: write

# Two release tags pushed close together must not race two runs that both force-move the mutable
# major tag with no ordering guarantee about which SHA wins.
concurrency:
  group: vault-action-release
  cancel-in-progress: false

jobs:
  release:
    name: Build, test, and move the major tag
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # This workflow's own last step force-pushes the mutable major tag (e.g. vault-action-v1),
      # which matches this workflow's own `vault-action-v*` trigger and would otherwise spawn a
      # second, guaranteed-failing run against a tag that isn't MAJOR.MINOR.PATCH. Detect and skip
      # that self-triggered run cleanly (success, not failure) before doing any other work.
      - name: Check whether this is a full release tag (vs. the major-tag move triggering itself)
        id: tagcheck
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          if [[ "$TAG" =~ ^vault-action-v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "is_full_release=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_full_release=false" >> "$GITHUB_OUTPUT"
            echo "Skipping: '$TAG' is not a MAJOR.MINOR.PATCH release tag — this is the major-tag move this workflow just performed, not a new release."
          fi

      - uses: actions/checkout@v7
        if: steps.tagcheck.outputs.is_full_release == 'true'
        with:
          fetch-depth: 0

      - name: Setup Node.js
        if: steps.tagcheck.outputs.is_full_release == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Setup pnpm
        if: steps.tagcheck.outputs.is_full_release == 'true'
        uses: pnpm/action-setup@v6
        with:
          version: 11.9.0

      - name: Install dependencies
        if: steps.tagcheck.outputs.is_full_release == 'true'
        run: pnpm install --frozen-lockfile

      - name: Build vault-action (and its @project-vault/agent dependency)
        if: steps.tagcheck.outputs.is_full_release == 'true'
        run: pnpm --filter "@project-vault/vault-action..." build

      - name: Test vault-action
        if: steps.tagcheck.outputs.is_full_release == 'true'
        run: pnpm --filter @project-vault/vault-action test

      # AC-15 — re-runs the SAME freshness check ci.yml already enforces at PR time, but here
      # against the exact commit this tag points at, so a tag pushed from a commit where dist/
      # had drifted from src/ (e.g. a manual commit that bypassed PR review) never moves the
      # mutable major tag.
      - name: Re-verify dist/ freshness at the tagged ref (D6/AC-11/AC-15)
        if: steps.tagcheck.outputs.is_full_release == 'true'
        run: pnpm check-vault-action-dist

      - name: Determine major-version convenience tag
        if: steps.tagcheck.outputs.is_full_release == 'true'
        id: major-tag
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          # e.g. vault-action-v1.2.3 -> vault-action-v1
          if [[ ! "$TAG" =~ ^vault-action-v([0-9]+)\.[0-9]+\.[0-9]+$ ]]; then
            echo "ERROR: tag '$TAG' does not match the required vault-action-vMAJOR.MINOR.PATCH format" >&2
            exit 1
          fi
          MAJOR="${BASH_REMATCH[1]}"
          echo "full_tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "major_tag=vault-action-v${MAJOR}" >> "$GITHUB_OUTPUT"

      - name: Move the major-version tag (e.g. vault-action-v1) to this commit
        if: steps.tagcheck.outputs.is_full_release == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -f "${{ steps.major-tag.outputs.major_tag }}" "${{ steps.major-tag.outputs.full_tag }}"
          git push origin "${{ steps.major-tag.outputs.major_tag }}" --force

      # Issue #112 / GitHub Marketplace requires action.yml at a repo ROOT — impossible for this
      # monorepo. nestormata/vault-action is a build-mirror-only repo (no independent source, no
      # PRs) that this step publishes the just-verified action.yml + dist/ to, tagged to match this
      # release, so `nestormata/vault-action@v1` / `@v1.0.0` resolve there. Source of truth stays
      # in this monorepo's packages/vault-action.
      - name: Mirror action.yml + dist/ to nestormata/vault-action
        if: steps.tagcheck.outputs.is_full_release == 'true'
        env:
          GH_TOKEN: ${{ secrets.VAULT_ACTION_MIRROR_TOKEN }}
        run: |
          set -euo pipefail
          VERSION_TAG="${{ steps.major-tag.outputs.full_tag }}"        # e.g. vault-action-v1.0.0
          MIRROR_TAG="v${VERSION_TAG#vault-action-v}"                   # e.g. v1.0.0
          MIRROR_MAJOR_TAG="${{ steps.major-tag.outputs.major_tag }}"
          MIRROR_MAJOR_TAG="v${MIRROR_MAJOR_TAG#vault-action-v}"        # e.g. v1

          git clone "https://x-access-token:${GH_TOKEN}@github.com/nestormata/vault-action.git" /tmp/vault-action-mirror
          cd /tmp/vault-action-mirror
          rm -rf dist action.yml
          cp -r "$GITHUB_WORKSPACE/packages/vault-action/action.yml" action.yml
          cp -r "$GITHUB_WORKSPACE/packages/vault-action/dist" dist

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add action.yml dist
          if git diff --cached --quiet; then
            echo "No build changes to mirror for $MIRROR_TAG (README/LICENSE unaffected)."
          else
            git commit -m "Mirror ${VERSION_TAG} build from nestormata/project-vault@${GITHUB_SHA}"
            git push origin main
          fi

          git tag -f "$MIRROR_TAG"
          git push origin "$MIRROR_TAG" --force
          git tag -f "$MIRROR_MAJOR_TAG" "$MIRROR_TAG"
          git push origin "$MIRROR_MAJOR_TAG" --force

          gh release create "$MIRROR_TAG" \
            --repo nestormata/vault-action \
            --title "$MIRROR_TAG" \
            --notes "Mirrored from nestormata/project-vault@${GITHUB_SHA}. See release notes on that repository for details." \
            --latest || echo "Release $MIRROR_TAG may already exist on the mirror — check manually."
